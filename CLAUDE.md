<div dir="rtl">

# CLAUDE.md

> **اقرأ [`AGENTS.md`](AGENTS.md) أولاً قبل أي شيء آخر.**

هذا المستودع مشروع «محفظة الجنوب» — محفظة مالية رقمية عربية (RTL) بمعيار إنتاجي، و`AGENTS.md` هو **ميثاقك كمالك لهذا المشروع** وليس قائمة أوامر: كيف تبنيه في خيالك، وكيف تفرّق الصحيح من الخطأ، وكيف تكشف الأخطاء بنفسك، وكيف تتحقق قبل أن تدّعي الإنجاز، وما حالة المشروع الآن بصدق.

الخلاصة العملية (والتفصيل في AGENTS.md):

- **المنتج:** Next.js 16 واحدة خادمة+واجهة، TypeScript صارم، Prisma+SQLite، دفتر مزدوج القيد (Σ=0) لكل عملية مالية، مصادقة TOTP بلا SMS، عربية RTL بهوية ذهبية/سوداء بلا أزرق.
- **ابدأ دائماً من:** `AGENTS.md` ثم `worklog.md` (آخر قسم) ثم وثائق `docs/` (المرجع الأعلى: `docs/MASTER_PLAN.md`).
- **قبل أي التزام:** `bun run lint` و`bun run typecheck` نظيفان + إثبات تشغيلي (curl/متصفح) + `ledger-check` متوازن إن لمست المال.
- **ممنوع من المستودع أبداً:** `.env*` الحقيقية، `db/*.db`، `uploads/*` (PII)، السجلات — ومجلدات بيئة التطوير (`upload/`, `.zscripts/`, `Caddyfile` الجذري…) أدوات بيئة لا كود منتج.
- **أنت المالك:** اعمل على ما يجب لا على ما يُطلب فقط، ووثّق أثرك في `worklog.md` بعد كل عمل.

</div>
