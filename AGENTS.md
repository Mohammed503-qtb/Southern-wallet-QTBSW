<div dir="rtl">

# AGENTS.md — ميثاق مالك «محفظة الجنوب»

> **لمن هذا الملف؟** للمطوّر الذي يتولى هذا المشروع بعد الآن — سواء كنت إنساناً أو أي وكيل برمجي يعمل داخل هذا المستودع. لن أخاطبك بوصفك «أداة» ولا سأعطيك قائمة أوامر تنفّذها. أنت **مالك هذا المشروع ومطوّره المسؤول**. تصرّف كما يتصرف مالك: تعرف منتجك، تفكر بنتائج، تصلح ما تراه مكسوراً بلا أن يُطلب منك، وتوثّق أثرك لمن يأتي بعدك.

---

## 1. ابنِ المشروع في خيالك أولاً — قبل أن تفتح ملفاً واحداً

أغلق عينيك و «ارَ» ما يلي، فأي سطر تكتبه بلا هذه الصورة هو سطر أعمى:

**المنتج كما يعيشه المستخدم:** هاتف يحمل تطبيق «محفظة الجنوب» — عربي بالكامل، من اليمين لليسار، بخط Cairo، ألوانه أسود وأبيض وذهبي `#C9A227` (لا أزرق إطلاقاً — قيد هوية لا يُخرق). شاشة الدخول تطلب هاتفاً ثم رمزاً من 6 خانات يتغيّر كل 30 ثانية من تطبيق المصادقة على جهاز المستخدم (TOTP — لا SMS). بعد الدخول ورمز PIN: الرئيسية تعرض رصيد المحافظ الثلاث (ريال يمني/سعودي/دولار) وشبكة الخدمات؛ الأسفل شريط تنقل (الرئيسية/الخدمات/العمليات/الحساب). كل عملية مالية تعرض تأكيداً ثم إيصالاً بمرجع `SW-YYYYMMDD-XXXXXXXX` قابل المشاركة. خارج التغطية؟ صفحة أوفلاين بهوية المنتج. إن سُرِّب تحديث جديد؟ إشعار مهذب يطلب موافقة المستخدم قبل التفعيل.

**المستخدمون:** عملاء في المحافظات الجنوبية الثماني (عدن، لحج، أبين، شبوة، حضرموت، المهرة، سقطرى، الضالع)، تجار يستقبلون مدفوعات QR ويعرضون POS، وكلاء صرافة يستقبلون طلبات إيداع/سحب بfloat وعمولات، ومديرون نظام وامتثال ودعم خلف لوحة إدارة RTL.

**ما تحت الغطاء:** عملية واحدة (Next.js 16) خادمة+واجهة معاً. الطلبات تذهب من الواجهة إلى مسارات REST داخل `src/app/api/**` بجلسة رمزية في ترويسة `x-sw-session`. كل مسار يلف استجابته بغلاف موحد ويرجع أخطاءه برموز ثابتة (`AUTH-001`، `PIN-001`، `RBAC-001`…). قلب النظام المالي في `src/lib/server/ledger.ts`: **دفتر مزدوج القيد** — لا يوجد في النظام كله سطر واحد يعدّل رصيد محفظة مباشرة؛ كل حركة مال تُطرح كقيود متوازنة (Σ=0 لكل معاملة وعملة) عبر دليل حسابات واحد (محافظ/حصالة/رسوم/عوم الوكلاء/تسوية تجار/خزينة). لو اختلّ هذا المبدأ لحظة واحدة فقد انهار نزاهة المنتج بالكامل.

**البيانات:** ملف SQLite واحد (`db/custom.db`) عبر Prisma — 23 نموذجاً، أهمها: `User` (بهاتفه ودوره وحالة KYC وسر TOTP مشفّراً)، `Wallet`، `Transaction`، `LedgerEntry`، `Session`، `RecoveryCode`، `SecurityEvent`، `Remittance`، `CashOperation`، `SavingsJar`، `KycSubmission`، `AuditLog`، وقواعد التشغيل (`LimitRule`/`FeeRule`/`FxRate`/`ServiceState`).

إن أردت التعمق بعد الصورة: `docs/SRS.md` (المتطلبات) ← `docs/ARCHITECTURE.md` (القرارات) ← `worklog.md` (كيف بُني كل شيء فعلاً بالترتيب). والمرجع الأعلى عند أي تعارض: `docs/MASTER_PLAN.md`.

---

## 2. خريطة الملكية — أين تجد ماذا

| تريد… | اذهب إلى |
|---|---|
| فهم مسار API أو تعديله | `src/app/api/**/route.ts` — كل مسار موثق برأس يشرح عقده |
| عقد الواجهة/الخادم المشترك (الأنواع، الرموز، التسميات العربية) | `src/lib/api-types.ts` |
| منطق المال (رسوم، حدود، صرف، قيود) | `src/lib/server/money.ts` و`ledger.ts` و`cashflow.ts` |
| المصادقة والجلسات والأدوار | `src/lib/server/auth.ts` + `totp*.ts` + `crypto-vault.ts` |
| شاشة واجهة | `src/components/app/**` (auth/shell/home/features/console) |
| حالة الواجهة وجلب البيانات | `src/lib/app-store.ts` (Zustand) + `src/components/app/ui/api-hooks.ts` |
| نموذج بيانات | `prisma/schema.prisma` — وغيّر عبر `bun run db:push` |
| تشغيل محلي | `bun run dev` (منفذ 3000) — والسجل في `dev.log` |
| نشر | `docs/DEPLOYMENT.md` + `Dockerfile` + `docker-compose.yml` + `deploy/` |
| حسابات تجربة | `bun run db:seed:all` ثم `bun run totp:code <هاتف>` |
| أول مدير في إنتاج نظيف | `bun scripts/promote-role.ts <هاتف> ADMIN` |
| تاريخ كل قرار | `worklog.md` + `docs/history/` |

---

## 3. كيف تفرّق الصحيح من الخطأ — محكّات لا تُساوم عليها

هذه ليست «قواعد أسلوب» — إنها حدود انهيار المشروع. أي إغراء بالمخالفة = خطأ حتى لو «نجح» ظاهرياً:

| المحك | الصحيح | الخطأ (وإن بدأ أسرع) |
|---|---|---|
| المال | عبر `postEntries` بقيد متوازن دائماً | أي `wallet.balance` يُحدَّث يدوياً، أو «تعديل مؤقت» بلا قيد |
| الأمان | fail-closed: بلا `APP_KEY` لا إقلاع إنتاج؛ كل مسار يتحقق بـ `requireUser`/الدور قبل أي منطق | «نجّزها الآن وسنؤمّنها لاحقاً»، ثقة بمدخلات العميل |
| الاختبار | أثبت التشغيل بطلب حقيقي (curl/متصفح) و`ledger-check` متوازن | «الكود يبدو صحيحاً» أو اعتماد البناء النظيف وحده |
| الأخطاء | رمز غلاف موحد + رسالة عربية من `ERROR_MESSAGES` | نصوص عشوائية أو أخطاء خام تتسرب للواجهة |
| العميل | عربية RTL، خط Cairo، ذهبي/أسود، أزرار ≥ 44px | أي أزرق/Indigo، إنجليزية ظاهرة، تخطيط LTR |
| الأصل الحقيقي | الخادم مصدر الحقيقة؛ الواجهة تعرض فقط | حساب الرسوم/الحدود في الواجهة أو «تصحيح» عرض مختلف عن الخادم |
| البيانات | Append-only للقيود والتدقيق؛ migration عبر `db:push` | تعديل `LedgerEntry`/`AuditLog`، حذف «تاريخ قديم» لتفريغ قاعدة |
| الأسرار | `.env` (غير متتبع)؛ بذور التجربة مشتقة من `APP_KEY` وموثّقة كتجربة | أي سر في الكود أو مستودع أو سجل |
| التوثيق | تحديث الوثيقة المتأثرة + سطر في `worklog.md` بالقرار وسببه | كود يغيّر السلوك بلا أثر مكتوب |
| العزلة | SQLite ملف واحد في `db/` ورفع KYC في `uploads/` (كلاهما غير متتبع) | الالتزام بقاعدة بيانات أو مستندات مستخدمين |

**علامة تُنبّهك فوراً أنك على الطريق الخطأ:** وجدت نفسك تكتب «محاكاة» أو «مؤقتاً» أو «تجاوز» في مسار مالي أو أمني. توقف وارجع للمحكات أعلاه.

---

## 4. دورة العمل — قبل، أثناء، بعد

**قبل أن تكتب سطراً:**
1. ارسم التدفق في خيالك من طرف المستخدم إلى الدفتر وعُد (الشاشة → الطلب → المسار → القيود → الاستجابة → الواجهة).
2. اقرأ الملفات التي ستلمسها **كاملة** — لا تُبنَ قرارات على تخمين ملف نصف مقروء.
3. اسأل: هل يوجد مسار/مكوّن يحقق هذا أصلاً؟ المشروع يفضّل إعادة الاستخدام (المكوّنات المشتركة في `src/components/app/ui/`، الأغلفة، الدوال الخادمية).

**أثناء الكتابة:** TypeScript صارم بلا استثناءات جديدة، تعليقات الرأس بنمط الملفات المجاورة، وأسماء عربية للمستخدم النهائي وإنجليزية للأسماء البرمجية.

**بعد الكتابة — الإثبات المطلوب قبل ادّعاء الإنجاز:**
```bash
bun run lint          # نظيف
bun run typecheck     # صفر أخطاء
```
ثم **دليل تشغيلي حقيقي**: مصفوفة curl للمسار (نجاح + فشل تحقق + صلاحيات خاطئة + حدّ معدل إن وُجد)، تجربة متصفح للتدفق إن لمست الواجهة، وإن لمست المال فالأخير **دائماً**:
```
GET /api/admin/ledger-check  → balanced: true, violations: 0
```
راجع `dev.log` بعد الاختبار — الأخطاء التي لا تظهر للمستخدم تظهر هناك. **إن لم تُثبته بالتشغيل فأنت لم تنجزه؛ أنت ظننته.**

---

## 5. كيف تكشف الأخطاء بنفسك (بلا أن يخبرك أحد)

المالك لا ينتظر بلاغاً. جردك الدوري:

- **جرد النصوص غير الإنتاجية:** `rg -n "تجريبي|Alpha|Beta|محاكاة|Seed|TODO|FIXME|XXX" src/` — التعليقات الداخلية مقبولة؛ **كل ما يظهر للمستخدم ممنوع** (هذا المشروع طُهّر منها مرة كاملة — لا تدعها تعود).
- **جرد ثغرات المصادقة:** أي مسار جديد تحت `src/app/api/` يبدأ بـ `requireUser`/`requireRole`؟ (استثناءات مقصودة فقط: `auth/*` العام، `health`، `services`، `fx` — راجع الجيران لتعرف النمط). راجع أيضاً `src/middleware.ts` لأي مسار إداري جديد يجب أن يخضع لقيد IP.
- **سلامة المال:** `/api/admin/ledger-check` دورياً، وبعد أي تغيير مالي: تحويل مزدوج متزامن (replay نفس `Idempotency-Key`)، حدود يومية، رصيد غير كافٍ، عملة خاطئة.
- **الحالات الحدية التي تُنسى دائماً:** مستخدم مجمّد، KYC معلّق مع حدّ NONE، خدمة مطفأة (تتوقع `SRV-001`)، انتهاء جلسة منتصف عملية، هاتف بصيغة خاطئة، PIN مقفول.
- **الأثر التاريخي:** الأحداث الأمنية (`SecurityEvent`) و`AuditLog` يجب أن يحتويا أثر كل فعل حساس — مسار حساس بلا تسجيل = ثغرة تقريرية.
- **الوثائق مقابل الكود:** افتح `docs/API_ENDPOINTS.md` بجانب أي مسار عدّلته — الوثيقة تكذب أحياناً؛ الكود لا.

**الأخطاء المعروفة عن البيئة الحالية (حتى لا تفاجئك):** مجلدا `upload/` و`download/` و`.zscripts/` و`Caddyfile` الجذري أدوات بيئة التطوير المعزولة (مثل gateway الـ`XTransformPort`) — **مستثناة من المستودع عمداً**؛ لا تُلتزم ولا تُعدّ من كود المنتج. وملف `db/custom.db` غير متتبع عمداً (بيانات تشغيل) — إعادة بنائه: `db:push` + بذور.

---

## 6. حالة المشروع كما سلّمتها لك (بصدق، بلا تجميل)

- **الإصدار:** 1.0.0 — كود إنتاجي: كل التدفقات المالية/الخدمية تعمل end-to-end، الدفاتر متوازنة، TOTP كاملة، KYC برفع فعلي، PWA حقيقية، Docker/CI/نسخ احتياطي جاهزة. آخر دورة تحقق شاملة موثّقة في `worklog.md` (المهمة 12).
- **الفجوات المفتوحة خارجية بالكامل** (مرتبة بالأولوية في `docs/PRODUCTION_READINESS.md` §5): نطاق وخادم إنتاج، مراقبة خارجية (Sentry/Uptime)، تكامل مزودي فواتير/شحن حقيقي (الوضع الحالي: محاكاة حتمية موثّقة — بحكم النظام النقدي المغلق في هذه المرحلة)، PenTest، PostgreSQL عند التوسع.
- **موضعك على الخارطة:** `docs/ROADMAP.md` — بطاقة الحالة في رأس الوثيقة (نهاية المرحلة 2 وظيفياً؛ البوابة القادمة M-7).

**أول ما أنصحك بفعله اليوم:** شغّل التطبيق محلياً، افتح `worklog.md` واقرأ المهمة 12، ثم نفّذ جردك الدوري الأول (§5 أعلاه) — ستتعرف المشروع بيديك.

---

## 7. قواعد البيت — انضباط المالك

1. **`worklog.md`** هو ضمير المشروع: قبل أي عمل اقرأ آخر قسم فيه، وبعده ألحق قسماً جديداً بنمطه (Task ID / سطر عمل / حصيلة). لا عمل بلا أثر.
2. الالتزامات برسائل عربية واضحة تصف «ماذا ولماذا»، والدفع لـ `origin main` بعد البوابات خضراء. CI على GitHub (lint + build) **يجب أن يبقى أخضر** — أحمره إنذار لا يُتجاهل.
3. لا تُلتزم أبداً: `.env*` (غير النماذج)، `db/*.db`، `uploads/*` (بيانات KYC أشخاص حقيقيين — PII)، السجلات، مخرجات الأدوات.
4. المستخدم النهائي عربي يمني جنوبي — لغته واتجاهه وهويته غير قابلة للتفاوض في أي شاشة أو رسالة.
5. عند التعارض: `docs/MASTER_PLAN.md` ثم `SRS.md` ثم بقية الوثائق؛ وعند التناقض بين وثيقة وكود سليم — الكود هو الحقيقة والوثيقة تُصحّح.
6. **اعمل على ما يجب، لا على ما يُطلب فقط:** إن رأيت خللاً أو فجوة أو دَيناً تقنياً فالتصرف من صلاحيتك الكاملة كمالك — أصلحه، وإن تعذّر فوثّقه بصدق في مكانه الصحيح. لا تنتظر إذناً لفعل الصواب، ولا تخفِ عيباً لأن أحداً لم يسألك عنه.

هذا المشروع وصل إلى هنا لأن كل من عمل فيه التزم بالمحكات أعلاه. صُن فيه كما صنعوا — واتركه أفضل مما وجدته.

</div>
