# محفظة الجنوب — South Wallet
## سجل العمل المشترك (Worklog)

هذا الملف هو سجل العمل الموحد لجميع الوكلاء العاملين على مشروع "محفظة الجنوب".
يجب على كل وكيل قراءته قبل البدء، وإلحاق سجله في نهايته بعد الانتهاء.

---

Task ID: 1
Agent: Main Agent (Z.ai Code)
Task: الخطوة 1 من 8 — كتابة وثيقة المتطلبات البرمجية الكاملة (SRS) لمشروع محفظة الجنوب، وعرضها في عارض ويب RTL أنيق

Work Log:
- قراءة وتحليل الملف المرفوع `upload/Pasted Content_1789156753860.txt` (Master PLAN.md — 3485 سطراً) بالكامل: قواعد المشروع الجوهرية (R-01..R-14)، الكيانات، الآلات الحالة، التدفقات، الأدوار الإدارية السبعة، وDoD.
- تحليل صورة الشعار `upload/IMG-20260911-WA0000.jpg` عبر VLM: نجمة هندسية ثمانية (Rub el Hizb)، أسود/أبيض أحادي، حركة S داخلية، أسلوب Minimal Geometric. نسخ الشعار إلى `public/logo.jpg`.
- كتابة وثيقة SRS كاملة (950 سطراً) في `docs/SRS.md` وفق IEEE 830 مكيّف: 11 قسماً رئيسياً + 3 ملاحق — تشمل 252 متطلباً وظيفياً (FR)، 40 متطلباً غير وظيفي (NFR)، 12 معيار قبول (AC)، 14 قاعدة حاكمة (R)، 8 مخاطر، حالات الأجهزة/المعاملات، مصفوفة الأدوار، وخريطة MoSCoW على المراحل (MVP/Beta/Launch).
- بناء عارض وثائق تفاعلي على المسار `/`:
  - `src/lib/doc-utils.ts`: استخراج فهرس + معرفات عناوين عربية موحّدة + إحصاءات الوثيقة (مشترك بين الخادم والعميل).
  - `src/components/srs-viewer.tsx` (عميل): react-markdown + remark-gfm مع مكونات مخصصة (جداول RTL برويسة دافئة، أكواد LTR داكنة، عناوين بشريط ذهبي)، فهرس جانبي لاصق مع تتبع القسم النشط عبر IntersectionObserver، فهرس جوال قابل للطي، شريط تقدم قراءة، زر طباعة، زر عودة للأعلى، غلاف بإحصاءات، فوتر لاصق سفلي.
  - `src/app/page.tsx` (خادم): قراءة docs/SRS.md عبر fs + تمريرها للعارض (force-dynamic).
  - `src/app/layout.tsx`: RTL عربي كامل + خط Cairo محلي (تنزيل woff2 عربي/لاتيني إلى `public/fonts/`).
  - `src/app/globals.css`: خطوط Cairo + شريط تمرير ذهبي مخصص + أنماط طباعة + تمرير سلس.
  - الهوية: أبيض/أسود + ذهبي الجنوب #C9A227 (بدون أزرق/Indigo) متسقة مع الشعار الأحادي.
- تشغيل `bun run lint` — نظيف بدون أخطاء.
- تثبيت `remark-gfm@4.0.1` لدعم جداول GFM.
- التحقق الشامل عبر Agent Browser: عرض سطح المكتب RTL سليم، التنقل بالفهرس (h يتوقف تحت الترويسة الثابتة)، 63 جدولاً و5 كتل كود و83 عنواناً مُعرّفة، الجوال 390px (فهرس قابل للطي + إغلاق تلقائي عند النقر)، الفوتر أسفل الصفحة، زر العودة للأعلى (أُنقل من يسار إلى يمين لتجنب شارة Next.js Dev)، بلا أخطاء Console.

Stage Summary:
- المخرجات: `docs/SRS.md` (وثيقة SRS v1.0 كاملة بالعربية) + عارض ويب جاهز على `/` + الشعار `public/logo.jpg` + خطوط `public/fonts/cairo-*.woff2`.
- قرار الهوية: أسود الجنوب #0B0B0C + أبيض + ذهبي الجنوب #C9A227 (مستمد من الشعار المرفق أحادي اللون)، بلا أزرق/Indigo — بانتظار الاعتماد النهائي في مرحلة نظام التصميم.
- القرارات المعمارية الموثقة في SRS: الخادم+Ledger مصدر الحقيقة، لا عمليات مالية Offline (FR-OFF-004: قائمة انتظار فقط)، Flutter إلزامي، NestJS مقترح (يُثبّت في الخطوة 3)، العملات الرقمية مؤجلة (FR-CRY Won't).
- الحالة الحالية: **الخطوة 1 (SRS) مكتملة — بانتظار تأكيد المستخدم للانتقال إلى الخطوة 2: User Stories.**
- الملفات المساندة للخطوات القادمة: `upload/Pasted Content_1789156753860.txt` (Master PLAN الحاكم)، `docs/SRS.md` (المرجع التعاقدي)، هذا الملف (سجل العمل).
