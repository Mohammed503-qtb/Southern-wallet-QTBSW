# محفظة الجنوب — South Wallet
## سجل العمل المشترك (Worklog)

هذا الملف هو سجل العمل الموحد لجميع الوكلاء العاملين على مشروع "محفظة الجنوب".
يجب على كل وكيل قراءته قبل البدء، وإلحاق سجله في نهايته بعد الانتهاء.

---

Task ID: 1
Agent: Main Agent (Z.ai Code)
Task: الخطوة 1 من 8 — كتابة وثيقة المتطلبات البرمجية الكاملة (SRS) لمشروع محفظة الجنوب، وعرضها في عارض ويب RTL أنيق

Work Log:
- قراءة وتحليل الملف المرفوع `upload/Pasted Content_1789156753860.txt` (Master PLAN.md — 3485 سطراً) بالكامل: قواعد المشروع الجوهرية (R-01..R-14)، الكيانات، الآلات الحالة، التدفقات، الأدوار الإدارية السبعة، وDoD.
- تحليل صورة الشعار `upload/IMG-20260911-WA0000.jpg` عبر VLM: نجمة هندسية ثمانية (Rub el Hizb)، أسود/أبيض أحادي، حركة S داخلية، أسلوب Minimal Geometric. نسخ الشعار إلى `public/logo.jpg`.
- كتابة وثيقة SRS كاملة (950 سطراً) في `docs/SRS.md` وفق IEEE 830 مكيّف: 11 قسماً رئيسياً + 3 ملاحق — تشمل 252 متطلباً وظيفياً (FR)، 40 متطلباً غير وظيفي (NFR)، 12 معيار قبول (AC)، 14 قاعدة حاكمة (R)، 8 مخاطر، حالات الأجهزة/المعاملات، مصفوفة الأدوار، وخريطة MoSCoW على المراحل (MVP/Beta/Launch).
- بناء عارض وثائق تفاعلي على المسار `/`:
  - `src/lib/doc-utils.ts`: استخراج فهرس + معرفات عناوين عربية موحّدة + إحصاءات الوثيقة (مشترك بين الخادم والعميل).
  - `src/components/srs-viewer.tsx` (عميل): react-markdown + remark-gfm مع مكونات مخصصة (جداول RTL برويسة دافئة، أكواد LTR داكنة، عناوين بشريط ذهبي)، فهرس جانبي لاصق مع تتبع القسم النشط عبر IntersectionObserver، فهرس جوال قابل للطي، شريط تقدم قراءة، زر طباعة، زر عودة للأعلى، غلاف بإحصاءات، فوتر لاصق سفلي.
  - `src/app/page.tsx` (خادم): قراءة docs/SRS.md عبر fs + تمريرها للعارض (force-dynamic).
  - `src/app/layout.tsx`: RTL عربي كامل + خط Cairo محلي (تنزيل woff2 عربي/لاتيني إلى `public/fonts/`).
  - `src/app/globals.css`: خطوط Cairo + شريط تمرير ذهبي مخصص + أنماط طباعة + تمرير سلس.
  - الهوية: أبيض/أسود + ذهبي الجنوب #C9A227 (بدون أزرق/Indigo) متسقة مع الشعار الأحادي.
- تشغيل `bun run lint` — نظيف بدون أخطاء.
- تثبيت `remark-gfm@4.0.1` لدعم جداول GFM.
- التحقق الشامل عبر Agent Browser: عرض سطح المكتب RTL سليم، التنقل بالفهرس (h يتوقف تحت الترويسة الثابتة)، 63 جدولاً و5 كتل كود و83 عنواناً مُعرّفة، الجوال 390px (فهرس قابل للطي + إغلاق تلقائي عند النقر)، الفوتر أسفل الصفحة، زر العودة للأعلى (أُنقل من يسار إلى يمين لتجنب شارة Next.js Dev)، بلا أخطاء Console.

Stage Summary:
- المخرجات: `docs/SRS.md` (وثيقة SRS v1.0 كاملة بالعربية) + عارض ويب جاهز على `/` + الشعار `public/logo.jpg` + خطوط `public/fonts/cairo-*.woff2`.
- قرار الهوية: أسود الجنوب #0B0B0C + أبيض + ذهبي الجنوب #C9A227 (مستمد من الشعار المرفق أحادي اللون)، بلا أزرق/Indigo — بانتظار الاعتماد النهائي في مرحلة نظام التصميم.
- القرارات المعمارية الموثقة في SRS: الخادم+Ledger مصدر الحقيقة، لا عمليات مالية Offline (FR-OFF-004: قائمة انتظار فقط)، Flutter إلزامي، NestJS مقترح (يُثبّت في الخطوة 3)، العملات الرقمية مؤجلة (FR-CRY Won't).
- الحالة الحالية: **الخطوة 1 (SRS) مكتملة — بانتظار تأكيد المستخدم للانتقال إلى الخطوة 2: User Stories.**
- الملفات المساندة للخطوات القادمة: `upload/Pasted Content_1789156753860.txt` (Master PLAN الحاكم)، `docs/SRS.md` (المرجع التعاقدي)، هذا الملف (سجل العمل).
---
Task ID: 3
Agent: general-purpose
Task: كتابة وثيقة المعمارية docs/ARCHITECTURE.md

Work Log:
- قراءة worklog.md ثم docs/SRS.md كاملاً (951 سطراً): التركيز على §1.3، §1.6 (R-01..R-14)، §2.4 (C-01..C-10)، كل FR، §6 NFR (أهداف الأداء §6.1 حرفياً)، §8 التقنيات المقيدة، AC-01..AC-12، الملاحق.
- قراءة المرجع الحاكم Master PLAN (3485 سطراً) بالأقسام المطلوبة: §1–3، §14–17، §62–70، §71–77، §78–84، §96–98، §120–124، §125–141، §142–152.
- كتابة docs/ARCHITECTURE.md (743 سطراً) وفق الهيكل المطلوب كاملاً: بطاقة وثيقة (C4 مكيف + ADR)؛ §1 مقدمة؛ §2 جدول R-01..R-14 → التجسيد المعماري؛ §3 نظرة C4-Level 1 (رسم ASCII + 8 مكونات + قنوات)؛ §4 معمارية Flutter (4 طبقات + Riverpod مقابل Bloc + Secure Storage + Offline + Deep Links + Mockable)؛ §5 لوحة الإدارة RTL منفصلة على نفس REST API + MFA؛ §6 NestJS Modular Monolith: قرار ADR-002 + جدول 24 وحدة Bounded Contexts + قواعد التبعية (interfaces لا implementations) + قالب R-09؛ §7 محرك المعاملات: التدفق الذري 9 خطوات + آلة الحالة ASCII + معالجة الفشل بين الخطوات + سياسة المزود (Pending/Retry/Reverse/Refund §69) + Reversal/Adjustment؛ §8 الأمان (JWT 15د + Rotation + Reuse Detection، OTP خادمي، Argon2id، AES-256-GCM+KMS، Secret Manager، Rate Limiting، RBAC Guards، MFA)؛ §9 جدول Provider Adapters؛ §10 الأداء (SRS §6.1 حرفياً + توسع Stateless/Replica/Partitions وACID كأساس)؛ §11 النشر (3 بيئات C-10 + CI/CD ببوابة موافقة + Migrations versioned §134 + قائمة إطلاق §136 + Rollback §137)؛ §12 عزل السجلات الثلاثة §83 + كوارث §124 الستة بجدول Recovery Path؛ §13 سجل ADR (12 قراراً)؛ §14 اشتقاق الخطوتين 4 (DB) و5 (API)؛ خاتمة + جدول موافقة بنمط SRS.
- مراجعة وتصحيح: إعادة رسم مخطط §3 ليصبح تدفق التطبيق واللوحة عبر LB صحيحاً، إصلاح نصوص مكسورة (الـ Saga، مع Hold، تلك الوثيقتان، وقيد مزدوج، التوافق بدل التعارض)، تصويب عبارة Boot-time Guard لقواعد الإنتاج §140.
- التحقق: 743 سطراً ضمن 700–950؛ رسوم ASCII فقط داخل كتل كود؛ جداول GFM؛ لا HTML/Mermaid؛ لا كود تطبيق فعلي (زائف فقط)؛ لم تُعدَّل ملفات أخرى؛ لم تُشغَّل خوادم أو build/lint.
- نسخ سجل العمل في worklog-entries/task-3.md.

Stage Summary:
- المخرجات: docs/ARCHITECTURE.md v1.0 (743 سطراً) — وثيقة المعمارية التقنية الكاملة لمحفظة الجنوب.
- أهم 3 قرارات مثبتة: (1) حسم C-03 رسمياً: Node.js + NestJS (ADR-001) — TypeScript موحّد مع Flutter عبر OpenAPI codegen، بيئة واحدة، DI وحدات قوية. (2) Modular Monolith بحدود 24 وحدة صارمة قابلة للفصل لخدمات لاحقاً (ADR-002) — فريق صغير، ACID داخل process واحد، Deployment واحد، R-03. (3) Riverpod لإدارة حالة Flutter (ADR-010) مع توثيق بديل Bloc ومسوغات الاختيار.
- كل قرار مربوط بـ R-XX/C-XX من SRS أو قسم Master PLAN مرقّم؛ أهداف الأداء من SRS §6.1 حرفياً بلا اختراع.
- الحالة: الخطوة 3 مكتملة — بانتظار الاعتماد للانتقال إلى الخطوة 4 (وثيقة قاعدة البيانات) ثم 5 (وثيقة API)، وكلاهما يُشتق من جدول الوحدات §6.2.
---
Task ID: 4
Agent: general-purpose
Task: كتابة مخطط قاعدة البيانات وERD docs/DATABASE_ERD.md

Work Log:
- قراءة worklog.md وdocs/SRS.md كاملة (§3 FR-GEO، §4.1–4.19، §5 FR-ADM، ملحق أ) والمرجع الحاكم upload/Pasted Content_1789156753860.txt بالأقسام المطلوبة (§4–§13، §14–§17، §18–§33، §36–§41، §43–§47، §54–§57، §62–§64، §71–§77، §89–§91، §99–§102، §105، §121، §123) مع القسم 71 (قائمة الكيانات الإلزامية) كأساس للتغطية الحرفية.
- تصميم 9 مجموعات (Domains) توزّع 78 جدولاً: الهوية والحساب (10)، الجغرافيا والأهلية (3)، المالية الأساسية Wallet+Ledger (6)، المعاملات والطلبات (13)، الشبكات والوكلاء والتجار (11)، الخدمات (13)، الدعم والمحتوى (7)، الإدارة والصلاحيات (9)، الأمن والتدقيق والتسوية (6).
- كتابة docs/DATABASE_ERD.md (2613 سطراً) وفق الهيكل الإلزامي: بطاقة وثيقة (المحرك PostgreSQL 15+) + مبادئ التصميم (16 قاعدة مربوطة بR-01..R-14 وأقسام Master) + خريطة المجموعات + ERD ASCII واحد شامل + جدول علاقات مرقّم (98 علاقة) + 45 CREATE TYPE + DDL كامل لكل جدول (عنوان + شرح عربي + كتلة SQL بالفهارس والقيود المسماة) + 22 قيد FK عبر المجموعات بALTER TABLE + آلات الحالة كقيود (16 آلة من ملحق أ) + استراتيجية الفهارس (28 فهرساً) والتقسيم الشهري Declarative Partitioning لtransactions/ledger_entries + سياسة الأرشفة (القسم 121) + سياسات البيانات والامتثال (تشفير/احتفاظ/حذف حساب/نسخ احتياطي §123) + 16 قراراً نمذجياً + جدول الموافقة بنمط SRS.
- قرارات جوهرية موثقة: Double-Entry على دليل حسابات ledger_accounts واحد (محافظ/حصالة/رسوم/AGENT_FLOAT/عمولات/تسوية تجار/PROVIDER_SUSPENSE/TREASURY)؛ المفتاح المركب (id, tx_date) للجدولين المقسّمين شهرياً مع جدول transaction_references غير مقسّم يضمن تفرد المرجع SW-YYYYMMDD-XXXXXXXX عالمياً ويربط REVERSAL بالأصل (§73/§75)؛ agent_float_accounts كرابط تشغيلي نحو حساب AGENT_FLOAT مع كاش يطابقه Reconciliation (§100)؛ admin_users منفصلون تماماً عن users (MFA + قيود IP)؛ لقطة الرسوم fee_rule_id+fee_version+fee_amount داخل المعاملة (§37)؛ metadata JSONB لقطة كاملة (§74)؛ Append-only بREVOKE UPDATE/DELETE على الجداول المالية وaudit_logs؛ soft delete للمحتوى فقط (§90).
- التحقق الآلي بدون إنشاء أي قاعدة بيانات: تغطية كيانات القسم 71 = 56/56 (صفر نواقص) + كيانات SRS الإضافية 22/22؛ 82 كتلة SQL / 195 عبارة تم تحليلها نحوياً بمحلل PostgreSQL الحقيقي (pglast 8.4) — كلها سليمة؛ صفر أسماء قيود مكررة؛ كل REFERENCES/ALTER TABLE/CREATE INDEX تشير لجداول معرّفة؛ كل أنواع ENUM الـ45 مستخدمة في أعمدة فعلياً.
- حفظ سجل العمل في worklog-entries/task-4.md ثم إلحاقه هنا.

Stage Summary:
- المخرجات: docs/DATABASE_ERD.md (وثيقة DDL كاملة للمنتج النلائي على PostgreSQL 15+) — 2613 سطراً، 78 جدولاً، 45 نوع ENUM، 98 علاقة مرقّمة، 22 قيد ALTER، 45 فهرساً داخل كتل DDL، 4 أقسام شهرية نموذجية + DEFAULT partitions.
- التغطية: كل كيانات القسم 71 من Master PLAN مغطاة حرفياً (صفر نواقص) + كل الإضافات الإلزامية من SRS (otp_codes, idempotency_keys, remittance_orders/deliveries, favorites, exchange_rates, agent_float_accounts, agent_commission_rules, agent_daily_settlements, merchant_settlements, network_cards/sales, push_tokens, admin_users/admin_sessions, provider_webhook_events, adjustment_requests, risk_rules/risk_events) + services وtransaction_references وsystem_state بمبرر §33/§73/§51.
- ملاحظة: الطول تجاوز المستهدف (1000–1300 سطراً) لأن تغطية DDL الكاملة لـ 78 جدولاً (الحد الأدنى المفروض من القوائم الإلزامية) تتطلب ذلك — الاكتمال والصحة النحوية قُدّما على الاختصار.
- للخطوات التالية: اعتماد الوثيقة، ثم وثيقة المعمارية/API وترتيب Migrations المقترح الموثق في §10.1 (ENUMs → D2 → D1 → D8 → D3 → D5 → D4 → D6 → D7 → D9 → قيود §5.10) مع Seed للمحافظات الثماني والعملات والأدوار السبعة.
- لم تُنشأ أو تُعدَّل أي قاعدة بيانات فعلية ولم تُشغَّل prisma/db — وثيقة تصميم فقط.
---
Task ID: 2
Agent: general-purpose
Task: كتابة وثيقة قصص المستخدمين docs/USER_STORIES.md

Work Log:
- قراءة worklog.md (الخطوة 1: SRS مكتملة) ثم قراءة docs/SRS.md كاملة (950 سطراً): الأدوار (§2.3)، المتطلبات الوظيفية FR-GEO/FR-AUTH/FR-KYC/FR-WLT/FR-LDG/FR-TRF/FR-REM/FR-PAY/FR-BIL/FR-TOP/FR-CWD/FR-SAV/FR-NWC/FR-STM/FR-NTF/FR-FAV/FR-SUP/FR-ACC/FR-OFF/FR-ADM، والملحقات أ/ب/ج (خريطة MoSCoW على MVP/Beta/Launch).
- قراءة المرجع الحاكم Master PLAN (upload/Pasted Content_1789156753860.txt): الأقسام 5–33 (المستخدم والعمليات المالية) و42–48 (الإدارة والأدوار والصلاحيات) و56–57 (الدعم وWhatsApp) و99–102 (الوكلاء والFloat والتجار والتسوية) إضافة إلى 58–60 و73 و125–127.
- تصميم خطة 85 قصة: CUST 18 / AGT 9 / MRC 8 / SAD 7 / FIN 9 / OPS 9 / SUP 6 / CON 6 / SEC 9 / AUD 4 — تغطية كل بنود التغطية الإلزامية حرفياً مع دمج محكم للبنود المتجاورة (QR+POSCOF، شحن+كروت الشبكة، أمان الحساب+إغلاقه) للبقاء داخل النطاقات العددية.
- قواعد الاتساق: الأولوية حرفياً من SRS والمرحلة من ملحق ج (قرار موثق: قصص FR-PAY/FR-BIL/FR-TOP/FR-SAV/FR-NWC أولوية Must ومرحلة Beta وفق ملحق ج).
- كتابة docs/USER_STORIES.md على 5 دفعات (Write + 4 إلحاقات ذرّية عبر cat >> heredoc): البطاقة والمقدمة وجدول الأدوار، ثم قصص الأدوار العشرة، ثم خريطة التتبع (13) وجدول الموافقة الختامي.
- تحقق آلي: 85 قصة (18/9/8/7/9/9/6/6/9/4)؛ 85 كتلة معايير قبول و85 صفاً «الربط بـ SRS»؛ الأولويات Must 80 / Should 4 / Could 1؛ المراحل MVP 71 / Beta 14؛ صيغة المرجع SW-YYYYMMDD-XXXXXXXX سليمة؛ قراءة عينات من البداية والوسط والخاتمة.
- حفظ نسخة السجل في worklog-entries/task-2.md ثم إلحاقه هنا.

Stage Summary:
- المخرجات: docs/USER_STORIES.md (v1.0) — 1415 سطراً، 85 قصة مستخدم عربية وفق INVEST بالنسق القياسي (القصة/الأولوية/المرحلة/الربط بـ SRS + 3–5 معايير قبول قابلة للاختبار تشمل سلوك الفشل والانقطاع والتكرار).
- البنية: بطاقة الوثيقة + مقدمة (منهجية واصطلاحات) + جدول الأدوار العشرة بتسميات SRS حرفياً + 10 أقسام قصص + خريطة تتبع (مصفوفة الدور→FR + جدول FR غير المغطاة: FR-CRY Won't وFR-TRF-007 Won't وعناصر Launch المؤجلة وFR-GEO-015 كفجوة مقترحة + ملاحظات اتساق) + جدول الموافقة.
- القرارات: دمج بنود متجاورة للالتزام بالأعداد؛ قصص الدفع/التاجر Must/Beta بانتظار تأكيد صاحب المنتج؛ US-CUST-008 = Should/MVP مرهونة بعقد تكامل شبكة الصرافة (A-03).
- ملاحظة: الطول الفعلي 1415 سطراً (الهدف كان 700–900)؛ الحد الأدنى الحسابي لـ85 قصة بالنسق الإلزامي ≈1200 سطر، فقدّم الالتزام بالأعداد والنسق والتغطية على هدف الطول — موثق في worklog-entries/task-2.md.
- لم يُكتب كود تطبيق ولم تُعدَّل ملفات أخرى ولم تُشغَّل خوادم أو أوامر build/lint.
- الحالة: **الخطوة 2 (User Stories) مكتملة — بانتظار اعتماد صاحب المنتج للقصص قبل الانتقال إلى الخطوة 3 (وثيقة المعمارية).**
---
Task ID: 5-b
Agent: general-purpose
Task: كتابة وثيقة الشاشات والتدفقات docs/SCREENS_FLOWS.md

Work Log:
- قراءة worklog.md وdocs/SRS.md (§1.6 القواعد، §4.1–4.19 كل FR، §6.4، §7 كاملاً: الهوية/الشاشات/الحالات/UX/إمكانية الوصول) وdocs/USER_STORIES.md (عناوين US-CUST/US-AGT/US-MRC) والمرجع الحاكم Master PLAN (§6–8، §19–24، §55–57، §59/60، §84–88، §92–95، §105–110، §114، §142، §144، §145).
- التحقق من غياب docs/API_ENDPOINTS.md → اعتماد المجموعات المنطقية EP-BOOT/AUTH/KYC/GEO/WLT/TRF/REM/CASH/PAY/TOP/BIL/SAV/TRX/NTF/SUP/ACC/AGT/MRC بجدول تعريفي في §1.2.
- كتابة docs/SCREENS_FLOWS.md على 7 دفعات (Write + 6 Edit بمرسل حارس) وفق الهيكل الإلزامي حرفياً: بطاقة الوثيقة → §1 مقدمة (رموز + قاعدة الشاشة المالية) → §2 نظام التصميم (Design Tokens فاتح/داكن + 10 مكونات مشتركة) → §3 جدول الحالات الإلزامية (وفق SRS §7.3) + خريطة الحالات العالمية الثماني (Master §110) → §4 خريطة التنقل ASCII + الـStacks الخمسة + Deep Links (Master §55) → §5 النسق + جدول تغطية Master §85 + خريطة المعرفات → §6 الكتالوج 57 شاشة SC-01..SC-57 بالنسق السداسي في المجموعات الأربع عشرة → §7 التدفقات FL-01..FL-15 بخطوات مرقمة وأسهم ↓ وفروع → §8 قواعد UX المالية العابرة (15 قاعدة) → §9 إمكانية الوصول → جدول الموافقة.
- كل شاشة مربوطة عمودياً بـ FR من SRS + مجموعة API + قصة US حيث توجد؛ ترتيب الرئيسية حرفياً من Master §84؛ تفاصيل العملية من الخادم لا من التنقل (§86)؛ إعادة الجلب بعد التحويل (§87)؛ استعلام العملية الأصلية بعد الانقطاع (§114)؛ تذكرة مرتبطة + WhatsApp من المعاملة (§106/§107)؛ RTL من البداية (§92).
- تحقق آلي: 1324 سطراً؛ 57 شاشة فعلية؛ 15 تدفقاً؛ صفر مرسلات متبقية؛ 18 كتلة كود متوازنة؛ صفر أنماط Dart/HTML/mermaid؛ تغطية Master §85 = 35/35 (صفر غير مغطاة).
- حفظ السجل في worklog-entries/task-5b.md ثم هذا الإلحاق الذري والتحقق بالقراءة.

Stage Summary:
- المخرجات: docs/SCREENS_FLOWS.md v1.0 (1324 سطراً) — كتالوج 57 شاشة + 15 تدفقاً + نظام تصميم مختصر + حالات إلزامية وعالمية موحدة + خريطة تنقل وDeep Links.
- قرارات موثقة: (1) 57 شاشة بدل النطاق الإرشادي 38–45 لأن التعداد الإلزامي نفسه يفوقه — الاكتمال قُدّم على الرقم (موثق §5.3). (2) جدول الحالات الإلزامية من 12 حالة حرفياً من SRS §7.3 (المهمة ذكرت 11؛ المصدر يعدّ 12) — عرضت القائمة كاملة. (3) Pay/Merchant Payment شاشة واحدة SC-20؛ Digital Assets هيكل مخفي SC-57 وفق FR-CRY-003؛ إيداع/سحب الحصالة شاشة واحدة بوضعين وفق SRS §7.2. (4) قرار صيغة الأرقام: اللاتينية tabular افتراضياً مع دعم تنسيق عربي-هندي كخيار لاحق. (5) الطول 1324 سطراً (أرضية النسق الإلزامي لـ57 شاشة ≈630 سطراً) — الاكتمال قبل الطول كسوابق المشروع.
- لم يُكتب كود Dart/Flutter ولم تُعدَّل ملفات أخرى ولم تُشغَّل خوادم أو build/lint (فقط cat >> لإلحاق السجل وفق التعليمات + wc/rg للتحقق القرائي).
- الحالة: **الخطوة 5-ب (وثيقة الشاشات والتدفقات) مكتملة — بانتظار اعتماد صاحب المنتج؛ الخطوة الموازية 5-أ (API_ENDPOINTS.md) لم تكن موجودة عند التنفيذ ويُطلب مطابقة أسماء المسارات النهائية مع جدول EP- المنطقي في §1.2 عند اعتمادها.**
---
Task ID: 6
Agent: general-purpose
Task: كتابة خارطة الطريق docs/ROADMAP.md

Work Log:
- قراءة worklog.md (المكتمل: SRS/User Stories/المعمارية/قاعدة البيانات؛ API والشاشات بالتوازي فلم يُعتمد عليهما) ثم docs/SRS.md كاملة (950 سطراً): §1.6 R-01..R-14، §2.4/§2.5 (C/A)، §3.1، §4/§5 كل عائلات FR، §6.1، §7.1–§7.3، §9 (DoD + AC-01..AC-12)، §10 (RK-01..RK-08)، ملحق ج (MoSCoW MVP/Beta/Launch).
- قراءة المرجع الحاكم Master PLAN بالأقسام الحاكمة: §127 DoD، §128 Phases 1–11 حرفياً، §129 ترتيب العمل الداخلي، §33 محرك الخدمات، §48 Dashboard، §69 سياسة فشل المزود، §83 عزل السجلات، §124 الكوارث، §134–§141 (Migration/البيئات/الإطلاق §136/Rollback §137/النسخة الأولى §138/الخدمات الخارجية §139/قواعد الإنتاج §140/الإدارة لا تتجاوز النظام §141)، §150 معيار الاكتمال، §151 قواعد التنفيذ، §152 النتيجة.
- تصفح ARCHITECTURE.md (§6.2 الوحدات الـ24، §10.2، §11، §12) وUSER_STORIES.md (§13.1 المصفوفة، §13.2/§13.3) لأحجام النطاق.
- كتابة docs/ROADMAP.md على 5 دفعات (Write + 4 إلحاقات ذرّية heredoc): البطاقة والمقدمة ونظرة عامة (جدول المراحل + Gantt نصي أشهر 1–12 + خريطة Phases→إصدارات + مسار الإصدارات)؛ المرحلة 0 (بيئات C-10، CI/CD، نظام التصميم SRS §7.1، عقود المزودين A-03، متاجر RK-07، قرارات D-01..D-06، معلم M-0)؛ المرحلة 1 (Phases 1–4 بجداول: نطاق FR/وحدات معمارية/شاشات SRS §7.2/إدارة مرتبطة/معلم خروج + ترتيب §129 + دورة Master §150 + AC-01..07 + الإدارة الأساسية إلزام §138 + Internal Alpha بخياريه ومخاطرهما وقاعدة §140 + خارج النطاق)؛ المرحلة 2 (Phases 5–7 + قاعدة §139 + مصفوفة جهوزية المزودين + Closed Beta بعدن + AC-08/12 + مراقبة لوحة §48 + مسار الدخول)؛ المرحلة 3 (Phase 10: Reconciliation/مخاطر/Backup/PenTest + استكمال Phase 9 + M-6 + خطة Rollback §137)؛ Beta العام (Phase 11 + تقييم NFR §6.1 + دعم ميداني A-05 + بوابة M-7 بقائمة §136 كاملة وAC 100% واستقرار أسبوعين)؛ الإطلاق (توسع الثماني بقرارات إقليمية FR-GEO + حملة + وكلاء/Float RK-05 + تفعيل خدمات Beta + Force Update §137 + M-8)؛ ما بعد الإطلاق (المؤجل ملحق ج + FR-CRY بطبقة Custody + تحسينات NFR + محرك Services §33)؛ ثم جدول التتبع الرئيسي والفريق والميزانية (بنطاقات USD مع إخلاء مسؤولية) وKPIs لكل مرحلة وسجل المخاطر RK-01..08 والحوكمة (§127/§151) والموافقة.
- تصويب وإثراء: إصلاح 3 أخطاء إملائية، توحيد المراجع، وإضافة أقسام داعمة (1.5، 2.5، 4.0-دورة §150، 4.8، 5.8، 6.5، تفصيل §136 في 7.4، مخطط التوسع في 8.1، 9.5، جدول التصعيد في §15).
- تحقق آلي: 716 سطراً (700–950)؛ 8 كتل كود متوازنة (ASCII فقط)؛ صفر HTML؛ 260 صفاً بالجداول؛ كل AC/RK/A وM-0..M-8 وD-01..D-06 متسقة؛ لا كود تطبيق، لا تعديل ملفات أخرى، لا تشغيل خوادم/build/lint.
- حفظ سجل العمل في worklog-entries/task-6.md ثم إلحاقه هنا.

Stage Summary:
- المخرجات: docs/ROADMAP.md v1.0 (716 سطراً) — خارطة الطريق المرحلية Delivery Roadmap (MVP → Beta → Launch): 7 مراحل كبرى، 9 معالم خروج (M-0..M-8)، 6 بوابات قرار لصاحب المنتج (D-01..D-06)، إجمالي تقديري 9 أشهر حتى اكتمال الإطلاق + ما بعد الإطلاق من الشهر 10.
- الالتزام الحاكم: خطة Master §128 (Phases 1–11) هي ترتيب البناء والخارطة تجمعها في إصدارات مطابقة لملحق ج: Phases 1–4 → المرحلة 1 (Internal Alpha، AC-01..07)، Phases 5–7 → المرحلة 2 (Closed Beta، AC-08/12)، Phase 10 + استكمال 9 → المرحلة 3، Phase 11 → Beta العام وبوابة الإطلاق (قائمة §136 + AC 100% + استقرار أسبوعين)، Phase 8 مؤجل بقرار FR-CRY، مع ترتيب §129 داخل كل Phase وDoD §127 لكل بند.
- قرارات تخطيطية بانتظار صاحب المنتج: D-01 نمط تشغيل Internal Alpha (أموال حقيقية صغيرة / نظام نقدي مغلق)؛ D-02 ترتيب قصص الدفع/التاجر Must/Beta؛ D-05 جدول التوسع الجغرافي وحجم حملة الإطلاق؛ D-06 العملات الرقمية FR-CRY بقرار مستقل.
- الحالة: **المهمة 6 مكتملة — ROADMAP بانتظار اعتماد صاحب المنتج وحسم D-01/D-02 لبدء المرحلة 0.**
---
Task ID: 5-a
Agent: general-purpose
Task: كتابة وثيقة نقاط النهاية docs/API_ENDPOINTS.md

Work Log:
- قراءة worklog.md (الخطوات 1–4 مكتملة) ثم docs/SRS.md كاملة (252 FR + 40 NFR + R-01..R-14 + ملحق ج للمراحل) وdocs/ARCHITECTURE.md كاملة (§6.2 جدول الوحدات الـ24، §7 محرك المعاملات، §8 الأمان JWT/Rotation/MFA، §9 Adapters).
- قراءة المرجع الحاكم بالأقسام الإلزامية: §16 Idempotency، §19 إخفاء المستفيد، §48 Dashboard، §52 الإصدارات، §55 Deep Links، §61 البحث المركزي، §67 انقطاع الشبكة، §70 إدارة المحاولات، §82 صيغة الخطأ، §96–98 Remote Config وحدوده، §105 المعلقة، §106/§107 تذكرة من العملية وWhatsApp، §132 API Principle، §141 الإدارة لا تتجاوز النظام — ومطالعة أسماء جداول DATABASE_ERD §2/§3.1 لاتساق الموارد.
- كتابة docs/API_ENDPOINTS.md (1061 سطراً) على 5 دفعات ذرّية وفق الهيكل الإلزامي حرفياً: بطاقة وثيقة بنمط SRS + §1 الأساسيات (Base URL/الرؤوس العامة/أنبوب §132/غلاف الاستجابة §82/Cursor pagination/المبالغ string+currency/كائن transaction بمرجع SW-YYYYMMDD-XXXXXXXX/أكواد HTTP الحصرية/الأدوار) + §2 59 كود خطأ في 7 عائلات (AUTH/GEO/TRF/TXN/ADM/SYS/HWK) + §3–§23 مجموعات API بالنسق الموحد (BOOT/AUTH/KYC/WLT/TRF/FAV/REM/CWD/PAY/TOP/BIL/NWC/SAV/STM/NTF/SUP/ACC/AGT/MRC/ADM/HOOK) وكل مجموعة بفقرة الوحدة المالكة والوحدات المرتبطة + §24 ستة أمثلة JSON كاملة (طلب/نجاح/فشل: OTP تسجيل، تنفيذ تحويل بIdempotency-Key وPIN proof، حالة تحويل بعد انقطاع، شحن Pending، إنشاء حوالة برمز تسليم، تجميد إداري بموافقة ثانية) + §25 مصفوفة التغطية وسطر الفجوات (صفر لوظائف MVP/Beta) + جدول الموافقة.
- قرارات عقدية: Quote→Confirm لكل تدفق مالي (R-10)؛ PIN proof token؛ الاستعلام عن نفس العملية بعد الانقطاع (§67/AC-04)؛ رفع المستندات عبر URL موقّعة؛ ملكية POST /merchant/pay-requests لوحدة merchant-payments (EP-MRC-04) دون ازدواج مع EP-PAY؛ Webhooks بHMAC وIdempotency بالمعرف الخارجي؛ السبب الإلزامي وMaker-Checker للإدارة ولا مسار set balance (R-05).
- ضبط العدد إلى 130 نقطة (الحد الأعلى للمستهدف 110–130) بدمج 4 صفوف إدارية (GET/PUT agents/{id}، currencies·fx-rates، services·feature-flags، app-versions·settings) وتصحيح كل الإحالات؛ وتصحيحات جودة (أعمدة EP-TOP-05، Idempotency-Key لكل POST ذي أثر مالي/طلبي، توحيد عمود التوثيق).
- التحقق الآلي: 130 صف نقطة بأعمدة سليمة؛ 59 كود خطأ؛ 48 سياج كود متوازن (21 كتلة JSON)؛ لا HTML/Mermaid؛ 25 قسماً + الخاتمة؛ المصطلحات الحرفية موجودة. حفظ سجل العمل في worklog-entries/task-5a.md ثم الإلحاق هنا.

Stage Summary:
- المخرجات: docs/API_ENDPOINTS.md v1.0 (1061 سطراً) — مواصفة REST/OpenAPI-ready كاملة: 130 نقطة نهاية (تطبيق 84 / وكيل 8 / تاجر 7 / إدارة 30 / Webhook 1) + 59 كود خطأ موحد + 6 أمثلة JSON تفصيلية + مصفوفة تغطية FR بصفر فجوات MVP/Beta.
- الحالة: الخطوة 5 (وثيقة API) مكتملة — بانتظار الاعتماد للانتقال إلى توليد OpenAPI 3.1 وبناء وحدات NestJS (ADR-011/ADR-002) مع Migrations من DATABASE_ERD §10.1 واختبارات عقود AC-01..AC-12.
---
Task ID: 7 (orchestrator)
Agent: Main Agent (Z.ai Code)
Task: تنفيذ الخطوات 2–7 كاملة (قصص المستخدمين، المعمارية، قاعدة البيانات+ERD، API، الشاشات والتدفقات، خارطة الطريق) عبر 6 وكلاء متخصصين، وتحويل عارض SRS الأحادي إلى بوابة وثائق متعددة المراحل، والتحقق الشامل

Work Log:
- قراءة المرجع الحاكم Master PLAN.md كاملاً (3485 سطراً، 151 قسماً) + وثيقة SRS (950 سطراً) + كود العارض الحالي لضمان الاتساق التام.
- تفويض 6 وكلاء general-purpose على دفعتين متوازيتين ببرومبتات تفصيلية (هيكل ملزم + اصطلاحات SRS حرفية + بروتوكول worklog):
  - الدفعة 1 (متوازية): Task 2 قصص المستخدمين، Task 3 المعمارية، Task 4 قاعدة البيانات+ERD.
  - الدفعة 2 (متوازية): Task 5-a نقاط API، Task 5-b الشاشات والتدفقات، Task 6 خارطة الطريق.
- المخرجات الوثائقية (docs/): USER_STORIES.md (1415 سطراً، 85 قصة/10 أدوار)، ARCHITECTURE.md (743 سطراً، 24 وحدة، 12 ADR، حسم C-03: NestJS Modular Monolith + Riverpod)، DATABASE_ERD.md (2612 سطراً، 82 CREATE TABLE، 46 ENUM، 98 علاقة، تغطية القسم 71 كاملة)، API_ENDPOINTS.md (1061 سطراً، 130 نقطة، 59 كود خطأ)، SCREENS_FLOWS.md (1324 سطراً، 57 شاشة SC، 15 تدفقاً FL، تغطية Master §85 كاملة)، ROADMAP.md (716 سطراً، 7 مراحل، 9 معالم، 6 بوابات قرار).
- تحويل العارض إلى بوابة وثائق متعددة (استبدال srs-viewer.tsx):
  - src/lib/doc-utils.ts: إحصاءات موسعة (قصص/شاشات/تدفقات/نقاط/ADR/جداول/ENUM/أسطر).
  - src/lib/docs-manifest.ts: بيان 7 وثائق + loadDocMetas/loadActiveDoc (قراءة خادمية).
  - src/components/markdown-content.tsx: عارض RSC خادمي (react-markdown + remark-gfm بنفس هوية SRS).
  - src/components/portal-header.tsx + portal-sidebar.tsx + portal-toc.tsx + reading-widgets.tsx: جزر تفاعلية (ترويسة+شريط رقائق جوال+فهرس جوال، شريط جانبي بملاحة الوثائق+فهرس بتتبع نشط عبر IntersectionObserver، شريط تقدم، زر عودة أعلى).
  - src/components/overview-view.tsx: لوحة نظرة عامة (بطاقات الخطوات السبع + مؤشرات KPI + بطاقة الخطوة 8 + القرارات المعلقة D-01/D-05/FR-CRY/دفع-تاجر).
  - src/app/page.tsx: تركيب خادمي (searchParams ?doc=) + غلاف لكل وثيقة بإحصاءاتها + تنقل سابق/تالي + تذييل لاصق mt-auto + Suspense بهيكل تحميل للمتن الثقيل.
  - layout.tsx: تحديث العنوان/الوصف.
- إصلاحان جوهريان أثناء التحقق: (1) استيراد Map من lucide يحجب Map العام فيفشل new Map() داخل مراقب التقاطع → أعيدت تسميته Map as MapIcon؛ (2) خطأ ترطيب متقطع عند إعادة تحميل الصفحات الثقيلة (api/database) سببه تمرير الأبناء الخادميين داخل مكوّن عميل مع البث المتدفق في dev → أعيدت هيكلة الصفحة لتركيب خادمي صرف مع جزر تفاعلية منفصلة + حد Suspense صريح (أثبتت التجارب أن الصفحة بدون أبناء خادميين داخل العميل لا تنتج الخطأ).
- bun run lint: نظيف. جميع المسارات الثمانية /?doc= ترجع 200.
- تحقق Agent Browser شامل: بنية RTL سليمة، فتح كل الوثائق، فهرس جانبي+جوال يعملان (إغلاق تلقائي، إيقاف العناوين تحت الترويسة الثابتة عند 96px)، تتبع القسم النشط يعمل، التنقل السابق/التالي، زر العودة للأعلى، الجوال 390px (رقائق الملاحة + الفهرس القابل للطي بلا فائض أفقي)، الطباعة متاحة.
- تحقق بصري عبر VLM (glm-5v-turbo) على 3 لقطات (نظرة عامة/وثيقة سطح مكتب/جوال): عرض كامل، RTL صحيح، هوية أسود+ذهبي متسقة، بلا عيوب بصرية.

Stage Summary:
- المخرجات النهائية: 7 وثائق هندسية كاملة في docs/ (~8800 سطر توثيقي) + بوابة وثائق تفاعلية على المسار / (نظرة عامة + 7 وثائق بتنقل وفهارس وإحصاءات حية).
- قرار معماري للبوابة: التركيب الخادمي الصرف مع جزر تفاعلية (أصلح خطأ الترطيب المتقطع وحسّن زمن العرض الأول مع هيكل تحميل للمتن).
- ملاحظة صادقة: يظهر أحياناً في وضع dev فقط تحذير ترطيب على أثقل صفحتين (database/api) عند إعادة التحميل السريع — React يتعافى منه ذاتياً بلا أي أثر وظيفي (كل التفاعلات مؤكدة تعمل بعده)، وهو صنف معروف من سلوك البث المتدفق في وضع التطوير ولا يظهر في التنقل الطبيعي (فتح جديد = صفر أخطاء مؤكدة).
- الحالة: **الخطوات 1–7 (التوثيق الكامل) مكتملة — بانتظار اعتماد صاحب المنتج للوثائق والقرارات المعلقة (D-01، مرحلة الدفع/التاجر، FR-CRY، D-05) قبل بدء الخطوة 8: البناء والتنفيذ (MVP).**

---
Task ID: 8-0
Agent: Main Agent (Z.ai Code)
Task: تأسيس بناء MVP (الخطوة 8) بعد موافقة صاحب المنتج على كل شيء — المخطط، العقد، أساس الواجهة

Work Log:
- قراءة worklog.md (الخطوات 1–7 مكتملة) وقرارات ROADMAP المرحلة 1 (Internal Alpha) ورموز التصميم من SCREENS_FLOWS §2.1.
- تحويل نية المستخدم "أوافق على كل شيء ابدا التنفيذ حالا" إلى قرارات محسومة: D-01 نظام نقدي مغلق تجريبي، خدمات المرحلة 2 = COMING_LATER عبر ServiceState، FR-CRY خارج النطاق، 8 محافظات جنوبية فقط.
- كتابة prisma/schema.prisma كاملاً (21 جدولاً SQLite): User/OtpCode/Session/KycSubmission/Wallet/Transaction/LedgerEntry/Remittance/AgentProfile/CashOperation/CommissionEntry/SavingsJar/Beneficiary/Notification/SupportTicket/TicketMessage/AuditLog/LimitRule/FeeRule/FxRate/ServiceState + دفع المخطط (db:push) وتوليد العميل.
- كتابة src/lib/api-types.ts (عقد الأنواع الكامل: عملات/محافظات/ملصقات عربية/غلاف الأخطاء/20+ نموذج عرض + formatMoney/maskPhone).
- كتابة src/lib/api.ts (عميل جلب بغلاف موحد + Idempotency-Key + إعادة استخدام مفتاح انقطاع الشبكة AC-04).
- كتابة docs/MVP_CONTRACT.md (العقد الملزم للوكلاء: قرارات، اصطلاحات، عقد 60+ نقطة نهاية، محرك الدفتر Σ=0، خريطة الشاشات، ملكية الملفات، قواعد الجودة).
- تحديث layout.tsx (عنوان/وصف Alpha + viewport) وpage.tsx (مستضيف WalletApp) + stub wallet-app.tsx (شاشة بناء مؤقتة).
- تثبيت qrcode + @types/qrcode للخدمة الخلفية.

Stage Summary:
- الأساس جاهز: قاعدة بيانات مدفوعة + عقد API ملزم + أنواع مشتركة + عميل واجهة + هيكل صفحة.
- التخطيط للتنفيذ: 8-a خلفي (API+Seed) ∥ 8-b هيكل الواجهة والهوية، ثم 8-c شاشات الميزات، ثم 8-d لوحات الأدوار، ثم 8-e تكامل وتحقق AC-01..AC-07.

---
Task ID: 8-a (+ إكمال 8-0)
Agent: Main Agent (Z.ai Code) — بعد انتهاء مهلة سياق وكيل 8-a
Task: استكمال الخلفية: Seed الكامل + إصلاح أخطاء مسارات + التحقق الشامل لـAPI

Work Log:
- وكيل 8-a أنجز قبل انتهاء مهلته: كل المسارات الـ38 (A1–X5 كاملة) + مكتبات الخادم الـ12 (envelope/auth/pin/domain/ledger/money/notify/audit/idempotency/views/pagination/cashflow) — لكن Seed توقف عند الخطوة 5/8.
- كتابة Seed كاملة (prisma/seed.ts ~1070 سطراً): محافظ النظام (FEE/SUSPENSE/FX) + 21 مستخدماً + 12 وكيلاً + أرصدة افتتاحية محسوبة لتنتهي عند الأهداف + سجل تاريخي لأحمد عبر 25 يوماً (إيداع مكتمل، تحويلات متبادلة، حوالة مدفوعة، حصالة مرتين، صرف USD→YER، سحب مكتمل، سحب ملغى باسترجاع، تحويل SAR، حوالة معلقة 884290، إيداع فاطمة المعلق 556677) + تذكرة دعم + 26 إشعاراً (11 غير مقروء) + عمولات الوكلاء + مزامنة العوم.
- إضافة معامل at? اختياري إلى notify() لدعم التواريخ التاريخية.
- إصلاح 3 أخطاء وقت تشغيل اكتُشفت بالاختبار: (1) typo idmKey→idemKey في cash+exchange، (2) idemKey في exchange، (3) استعلام findUncommitted عبر db خارج المعاملة في مسار النقدي → إرجاع الصف المُنشأ مباشرة.
- تحقق curl شامل ناجح: A1→A2→A5→A8→T1→T2 (+replay=true بنفس Idempotency-Key = AC-03✓)، R2→R3، G1→G2→G3 (إتمام إيداع فاطمة)، G4 (دفع حوالة 884290 بمرحلتي الاستعلام والتأكيد)، M1 (ledgerBalanced=true)، M4، AC-02✓ (TXN-001)، AC-06✓ (TXN-002 بتفاصيل الحد)، AC-07✓ (GEO-001)، W3 (صرف SAR→YER)، إنشاء سحب مع حجز فوري.
- تحقق واجهة 8-b عبر agent-browser: التطبيق يعمل (splash→login→home ببيانات حية: أرصدة/خدمات/آخر العمليات/شارة 5 إشعارات).
- إنشاء 26 stub للشاشات (features/money ×12، features/account ×13، console ×1) وربط screen-router بخريطة كاملة Record<ScreenKey, ReactNode> — التطبيق يظل مترجماً طوال عمل الوكلاء المتوازيين.
- bun run lint: نظيف. GET / = 200.

Stage Summary:
- الخلفية كاملة ومختبرة (كل السلاسل الذهبية تعمل). الواجهة الأساسية تعمل ببيانات حية.
- الجاهزية للتوازي: 8-c-1 (شاشات المال 12) ∥ 8-c-2 (شاشات الحساب 13) ∥ 8-d (لوحات الأدوار) — كلٌ يستبدل stubs ملكه فقط.

---
Task ID: 8-e (التكامل والتحقق الشامل)
Agent: Main Agent (Z.ai Code)
Task: استكمال ما تركته مهل الوكلاء + التحقق الشامل E2E للتسليم النهائي لـAlpha الداخلي

Work Log:
- استكمال ما أنجزته الوكلاء قبل انتهاء مهلها (رغم فشل تسليم تقاريرهم): 8-c-1 أنجز 12/12 شاشة مال (cash screens عبر مكون مشترك 541 سطراً)، 8-c-2 أنجز 13/13 شاشة حساب، 8-d أنجز اللوحة كاملة (شريط جانبي حسب الدور + 9 أقسام + بوابة وكيل ب3 أقسام + ConfirmDialog بسبب إلزامي).
- إصلاح خطأ صياغة في console-app.tsx (تفكيك useState مكسور) + إزالة نمط setState-in-effect المخالف لقواعد lint (Portal بفحص typeof document مباشر).
- تنظيف: حذف 8 ملفات بوابة الوثائق القديمة غير المستخدمة (portal-*, markdown-content, overview-view, coming-soon, console-placeholder) مع إبقاء docs-manifest/doc-utils (تستخدمهما مسارات X1/X2).
- إعادة تشغيل خادم dev بعد توقفه (كل شيء يعمل 200 + /api/health سليم).
- **تحقق E2E عبر Agent Browser (كل المسارات على localhost:3000):**
  - تطبيق العميل: دخول سريع → رئيسية ببيانات حية (أرصدة/خدمات/آخر العمليات/شارة إشعارات) → **تحويل كامل**: إدخال هاتف+مبلغ بلوحة الأرقام → مراجعة (إخفاء المستفيد مح*** • 770000••• + رسوم 62 + رصيد متاح) → PIN → نجاح بإيصال ReceiptCard كامل (SW-20260912-Q7TAEFZF + QR).
  - بوابة الوكيل: نظرة (عوم/عمولات/معلق) → طابور (طلباتي + دفع حوالة بالرمز) → **دفع حوالة E2E**: إنشاء حوالة كعميل (رمز 709450) → تحقق الوكيل بالرمز → تفاصيل المستلم → تأكيد الدفع → "سُلّمت الحوالة بنجاح" + عمولة مسجلة (+15 ر.ي) + الحالة PAID عبر API.
  - لوحة الإدارة: دخول مدير → KPIs حية (21 مستخدماً/12 وكيلاً/24 عملية/2 معلقة) + **بطاقة "الدفاتر متوازنة — Σ=0"** (97 قيداً، 35 مجموعة، صفر مخالفات بعد كل عمليات الجلسة).
  - الجوال 390px: دخول + رئيسية + تنقل سفلي بلا تمرير أفقي. سطح المكتب: إطار الهاتف + لوحة الهوية + تذييل لاصق.
  - عارض الوثائق داخل التطبيق: بطاقات الوثائق السبع بإحصاءاتها → فتح SRS بmarkdown كامل.
- bun run lint: نظيف. صفر أخطاء في آخر 100 سطر من dev.log.

Stage Summary:
- **MVP المرحلة 1 (Internal Alpha) مكتمل ويعمل**: 38 مسار API مختبرة (بما فيها Idempotency/AC-02/03/06/07)، 26 شاشة عميل (12 مال + 13 حساب + docs) ولوحات 4 أدوار (ADMIN/COMPLIANCE/SUPPORT/AGENT)، Seed واقعي (دفاتر متوازنة، سجل 30 يوماً، حسابات تجريبية 770000001/2/3/10/770100001-3).
- سيناريوهات القبول المؤكدة: AC-01 (إيداع وكيل→محفظة→دفاتر)، AC-02 (رصيد غير كافٍ TXN-001)، AC-03 (إعادة Idempotency نفس المرجع)، AC-04 (مفتاح إعادة الاستخدام في api.ts)، AC-05 (تحرير الرسوم M10 عبر القواعد)، AC-06 (حدود TXN-002 بتفاصيل)، AC-07 (GEO-001 لحساب صنعاء).
- الحالة: التطبيق جاهز للعرض على المسار / — Alpha الداخلي بنظام نقدي مغلق تجريبي (قرار D-01).

---
Task ID: 9-0 (orchestrator)
Agent: Main Agent (Z.ai Code)
Task: افتتاح المرحلة النهائية 9 (التسليم التجاري): إصلاح جلسة الاعتماد الحرج + عقد خدمات المرحلة 2، تمهيداً للتنفيذ المتوازي 9-b/9-c/9-d

Work Log:
- تشخيص خطأ حرج من dev.log: POST /api/auth/demo-login 200 ثم GET /api/me 401 متكرراً — السبب: كوكي SameSite=Lax يُحجب في سياق إطار المعاينة (طرف ثالث)، لا خلل في الخلفية (curl أكّد سلامة الكوكي).
- إصلاح جذري (قناة مزدوجة iframe-safe): auth.ts يعيد رمز الجلسة من createSession ويقرأه من ترويسة x-sw-session بجانب الكوكي؛ verify + demo-login يعيدان data.sessionToken؛ عميل api.ts يخزنه في localStorage (sw_token) ويرسله مع كل طلب؛ حفظه في demo-login.tsx وotp-screen.tsx؛ مسحه في logout. اختبار curl كامل: ترويسة فقط → 200 (كانت 401)، logout عبر الترويسة → 401 بعدها ✓.
- عقد المرحلة 2 (إضافات فقط): TxType +6 (BILL_PAY, TOPUP, CARD_PURCHASE, MERCHANT_PAY_OUT, MERCHANT_SALE_IN, REMIT_IN_CLAIM) مع ملصقاتها؛ USER_ROLES + MERCHANT؛ أنواع عرض جديدة (BillerView, BillPreviewView, BillPayResultView, TopupOperatorView, CardProductView, MerchantPublicView, QrPayPreviewView/ResultView, InboundRemittanceView, RemitInClaimResultView)؛ رموز أخطاء BIL-001/TOP-001/MRC-001/RIN-001.
- إصلاح خلل منطقي: "أبين" كانت في قائمتي داخل النطاق وخارجه معاً — أزيلت من OUT_OF_SCOPE_EXAMPLES.
- التوجيه: +8 مفاتيح شاشات (bills, bill-pay, topup, cards, my-qr, pay-merchant, merchant-pos, remit-in) في app-store + screen-router (مع 8 ملفات شاشات مؤقتة سيستبدلها الوكلاء)؛ bootstrap: دور MERCHANT يبقى في تطبيق الهاتف؛ service-catalog: الخدمات الخمس أصبحت group="money" بوجهات شاشات فعلية.
- bun run lint نظيف، GET / = 200.

Stage Summary:
- الاعتماد يعمل الآن في أي سياق (كوكي + ترويسة) — أساس كل التحقق القادم.
- العقد المشترك محدث مسبقاً لتجنّب تضارب الوكلاء المتوازيين؛ الملفات المشتركة (api-types, app-store, screen-router, service-catalog, api.ts, auth.ts) **مغلقة** أمام 9-b/9-c/9-d إلا بإذن صريح.
- التوزيع المتوازي: 9-b (فواتير+شحن+كروت) ∥ 9-c (دفع تاجر QR+حوالات واردة+بوابة التاجر) ∥ 9-d (PWA+إمكانية وصول+تلميع إنتاجي).
- عند اكتمال كل وكيل: يفعّل مفاتيح ServiceState الخاصة به فقط (BILLS/TOPUP/NETWORK_CARDS → 9-b؛ MERCHANT_PAY/REMITTANCE_IN → 9-c).
---
Task ID: 9-d
Agent: general-purpose (9-d)
Task: التلميع الإنتاجي والتغليف PWA — أيقونات ومانيفست وMetadata + تدقيق جودة الواجهة وإصلاحات safe-area/focus/aria في الملفات المملوكة فقط

Work Log:
- قراءة worklog.md كاملاً (الخطوات 1–9-0) والتزام خريطة الملكية: لم يُلمس أي ملف من ملفات 9-b/9-c أو الملفات المشتركة المغلقة.
- **PWA — الأيقونات**: كتابة `scripts/gen-icons.ts` (مجلد scripts جديد) وتشغيله بـ bun مع sharp 0.34.5: قصّ الهوامش البيضاء من `public/logo.jpg` → بلاطة الشعار 423×407، ثم تركيب على خلفية بيضاء متوسطة بتصغير Lanczos:
  - `public/icons/icon-192.png` و`icon-512.png` (purpose any): محتوى 72% + حواف دائرية بنصف قطر 22.5% (زوايا شفافة).
  - `public/icons/maskable-192.png` و`maskable-512.png`: full-bleed أبيض بمحتوى 56% (القطر 0.56×√2≈0.79 < 0.80 المنطقة الآمنة الدائرية) — يصمد أي قص من المشغّل.
  - `public/icons/apple-touch-icon.png` 180×180 مصمتة بلا شفافية (iOS يقص الزوايا بنفسه)، محتوى 66%.
  - `public/favicon.ico`: حاوية ICO بصيغ PNG بأحجام 16+32+48 (محتوى 94% للقراءة الدقيقة).
  - تحقق جودة عبر VLM على لوحة تماس على خلفية شطرنجية: حواف حادة بلا ضجيج JPEG، توسيط سليم، زوايا دائرية سليمة، شفافية حيث يجب.
- **PWA — المانيفست**: `public/manifest.json`: name «محفظة الجنوب»، short_name «الجنوب»، وصف عربي موجز، id/start_url/scope = «/»، display standalone، orientation portrait، dir rtl، lang ar، theme_color/background_color #0B0B0C، icons ×4 بأغراض any/maskable منفصلة (أفضل ممارسة من دمجها في أيقونة واحدة). قرار: **لا shortcuts** (الديب لينك محصور بـ/ فلا معنى لاختصارات تفتح الجذر) و**لا screenshot hints** (الشاشات خلف دخول والمسار الوحيد المسموح /).
- **PWA — layout.tsx**: manifest + appleWebApp (capable + statusBarStyle black-translucent + title) + icons (favicon.ico 48 + icon-192 + apple-touch 180) + formatDetection telephone:no + عنوان/وصف عربي محدّث «محفظة الجنوب — محفظة إلكترونية لجنوب اليمن (Alpha تجريبي)» + viewport أصبح بـ viewportFit:cover (ضروري لعمل env() للمناطق الآمنة) مع themeColor مزدوج عبر prefers-color-scheme (فاتح #F7F6F3 / داكن #0B0B0C — globals.css فيه رموز .dark دون تفعيل فعلي، فازدواج الوسم آمن ومستقبلي). تحقق curl لكل الوسوم: manifest + theme-color ×2 + apple-mobile-web-app + format-detection + روابط الأيقونات.
- **قرار: لا Service Worker** — لأسباب: (1) بيئة dev دائمة مع Fast Refresh والكاش سيقدّم واجهة قديمة أثناء التطوير النشط لوكلاء متوازيين، (2) قيود المسار الوحيد / يمنع تسجيل SW بنطاق آمن مستقل، (3) معايير تثبيت Chrome الحديثة تتحقق بالمانيفست+الأيقونات على localhost دون SW. التأجيل موثق للـBeta مع SW + Workbox عند وجود نطاق إنتاجي.
- **تدقيق جودة الواجهة عبر agent-browser**:
  - سطح المكتب 1440×900: دخول سريع بأحمد 770000001 من لوحة الهوية يعمل، الرئيسية ببيانات حية، لوحة الهوية 844px متطابقة مع قاعدة إطار الهاتف (bottom=872 لكليهما) والتذييل مثبت أسفل اللوحة بلا فجوة، لا تمرير أفقي، صفر أخطاء صفحة. VLM أكّد: التذييل محاذٍ، لا عيوب بصرية، هوية أسود/ذهبي بلا أزرق.
  - الجوال 390×844: صفر تمرير أفقي على الرئيسية والخدمات والسجل والإشعارات وحسابي وتدفق التحويل وشاشة الدخول وSheet الحسابات التجريبية، أزرار التنقل 78×64 (>44px)، لوحة الأرقام كلها ≥44، زر الإجراء 52px. دخول عبر Sheet الجوال + تسجيل خروج + إعادة دخول يعملون.
  - aria: الأزرار الأيقونية كلها موسومة («رجوع»، «حذف رقم»، «إخفاء الرصيد»)، nav بـaria-label وaria-current، شارة الإشعارات aria-label.
  - splash: نظيف أثناء العرض (لا وميض محتوى غير منسق) — راجع الملاحظة 1 أدناه للانتقال.
- **الإصلاحات (في ملفاتي فقط)**:
  - `globals.css` (**إضافات فقط، صفر حذف**): حلقة تركيز ذهبية موحدة `:where(…):focus-visible { outline: 2px solid #C9A227; outline-offset: 2px }` داخل @layer base (كي تبقى أدوات Tailwind مثل outline-none/outline-2 هي المتغلبة في المكونات التي حددت أسلوبها) — تحقق computed = 2px solid rgb(201,162,39) + تأكيد بصري VLM للحلقة الذهبية على تبويب السجل؛ احترام prefers-reduced-motion (إيقاف الحركات والانتقالات)؛ `overscroll-behavior-y: contain` + `text-size-adjust: 100%` (منع pull-to-refresh وتضخيم الخط الأفقي كسلوك تطبيق أصلي)؛ لون تحديد نص ذهبي ::selection.
  - `app-shell.tsx`: `pt-[env(safe-area-inset-top)] lg:pt-0` — إزاحة المحتوى تحت شريط حالة standalone الشفاف (black-translucent) على الجوال الحقيقي دون مساس بإطار سطح المكتب.
  - `app-frame.tsx`: تحديث جملة متقادمة في لوحة الهوية («الشاشات المالية تُنجز تباعاً في نطاق 8-c/8-d» → صياغة إنتاجية عن خدمات Beta الموسعة).
  - `bottom-nav.tsx`: **بلا تعديل** — كان مطابقاً أصلاً (pb env(safe-area-inset-bottom)، min-h-11، aria-label، aria-current، شارة aria).
- **ملاحظة تشغيلية**: أثناء الفحوص النهائية وُجد الخادم متوقفاً على 3000 (إعادة تشغيل من وكيل متوازٍ) — أُعيد تشغيله بـ bun run dev (نسخة واحدة PID 6550) وتحقق 200 بعدها.
- **التحقق النهائي**: `bun run lint` نظيف (exit 0). GET / = 200. /manifest.json يُخدَّم JSON سليماً (name/short_name/4 أيقونات). favicon.ico 200 (image/x-icon) وكل الأيقونات 200. جولة متصفح ختامية: الدخول يعمل، الرئيسية تُحمّل، التنقل السفلي يعمل على الجوال وسطح المكتب، العنوان محدّث، لا تمرير أفقي، لا أخطاء صفحة. لقطة ختامية للهوية على سطح المكتب + VLM: كل شيء سليم.

Stage Summary:
- المخرجات: حزمة PWA كاملة (6 ملفات أصول + manifest + metadata Next.js متكاملة + favicon.ico) + سكربت توليد قابل لإعادة التشغيل + إصلاحات جودة (focus-visible ذهبي موحد، safe-area أعلى، reduced-motion، منع pull-to-refresh) في الملفات المملوكة حصراً.
- التطبيق قابل للتثبيت الآن كـ PWA على Chrome/Android وiOS (إضافة للشاشة الرئيسية) دون Service Worker (مؤجل للـBeta وموثق السبب).
- **ملاحظات للمنسق (لم تُصلح — خارج ملكيتي)**:
  1. انتقال splash→الرئيسية يُظهر إطاراً فاتحاً شبه فارغ (~300ms) لأن `sw-fade-in` في screen-router/app-styles يبدأ من opacity:0 بعد قطع الشاشة السوداء — تحسين مقترح: crossfade لإخراج الـsplash في ملفات shell-router لاحقاً (ملف 8-0/8-b).
  2. أثناء عملي ظهرت أخطاء ترجمة عابرة من `bill-pay-screen.tsx:513` (ملف 9-b قيد الكتابة لحظتها) في وضع dev فقط — التطبيق بقى يعمل وكل الشاشات الأخرى سليمة.
  3. أزرار الإجراء الأساسية التي تستعمل `focus-visible:outline` دون لون ترث outline-color من `--ring` (رمادي 50%) — يمكن رفعها للذهبي في مكوّن مشترك مستقبلاً.
  4. شارة إشعارات التنقل السفلي تُعلن كجزء من اسم الزر («10الإشعارات») — تحسين صياغة اختياري لاحق.
- الحالة: **9-d مكتمل — التغليف PWA والتلميع الإنتاجي جاهزان، كل الفحوص خضراء (lint/curl/browser/VLM)، بلا أي كسر لوظيفة قائمة وبلا لمس ملفات الوكلاء المتوازيين.**
---
Task ID: 9-b
Agent: general-purpose (9-b)
Task: خدمات المرحلة 2 الثلاث كاملة (خلفية + واجهة): سداد الفواتير BILLS، شحن رصيد الهاتف TOPUP، كروت الشبكة NETWORK_CARDS

Work Log:
- قراءة worklog.md كاملاً (خصوصاً Task 9-0: قناة x-sw-session وعقد المرحلة 2) + MVP_CONTRACT.md (الغلاف/الأخطاء/Idempotency/محرك الدفتر §6) + دراسة الأنماط الذهبية حرفياً: مسارات transfers/remittances (route، requireUser، assertNotFrozen، assertInScope، verifyPin خارج المعاملة، FeeRule+computeFee، assertDailyLimit، postEntries Σ=0، Idempotency كامل، notify) وشاشات remittance-create/transfer (form→review→pin→result/fail، PinStep، ReviewCard، ReceiptCard، postMoney/AC-04، refreshMe، toast) وapi-hooks وmoney-shared.
- إنشاء src/lib/server/payments-catalog.ts (ملك 9-b): 12 مزوّداً يمنياً واقعياً في 5 فئات + 4 مشغلين (You/MTN/Sabafon/YTelecom ببادئاتهم) بفئات شحن ثابتة + 15 باقة كروت بيانات + دوال البحث/التحقق + مستحق محاكى **حتمي** (hash المزود:الرقم → 3,000..25,000 ر.ي) + dueLabel + generateCardCode (11 خانة من أبجدية بلا لبس) + جلب الرسوم الحية من FeeRule (دون مس money.ts المغلق).
- 5 مسارات API جديدة بنمط transfers الحرفي (كلها +Idempotency-Key وPIN وحدود وΣ=0):
  • GET /api/bills → BillerView[] بالرسوم الحية (50 ر.ي) + وضع ?ref= لاسترجاع metadata فاتورة.
  • POST /api/bills/preview → تحقق صيغة → BillPreviewView بمستحق حتمي + dueLabel (BIL-001 للمجهول).
  • POST /api/bills/pay → معاملة BILL_PAY (BP-…): [MAIN DEBIT a+f]+[FEE CREDIT f]+[SUSPENSE CREDIT a] → metadata {billerCode, billerName, accountNumber, dueAmountMinor, dueLabel} + notify + writeAudit → BillPayResultView. قيدان: المبلغ ≤ المستحق، وغير المفتوح (اتصالات/إنترنت/حكومي) سداد كامل فقط.
  • GET/POST /api/topup → مشغلون بفئات ورسوم (15 ر.ي)؛ POST يتحقق بادئة الرقم للمشغل + الفئة (TOP-001) → معاملة TOPUP (TU-…) بmetadata {phone, operatorCode, operatorName}.
  • GET/POST /api/cards → باقات (+feeMinor حي)؛ POST يولّد **رمز كرت 11 خانة** في metadataJson ويكشفه في الاستجابة والإشعار → معاملة CARD_PURCHASE (NC-…) — الرمز يُسترجع لاحقاً فقط عبر ?ref=.
- استثناءان نظاميان فقط على ملفات غير ملكي: (1) transactions/route.ts — إضافة BILL_PAY/TOPUP/CARD_PURCHASE إلى VALID_TYPES (الفلتر types= قائم) بلا مس سلوك؛ (2) transaction-details-screen.tsx — إضافة فقط: جلب metadata عبر ?ref= وصفوف «رقم الحساب/الرقم المشحون/الباقة» + بطاقة CardCodeBox داكنة بزر نسخ وتحذير لعمليات الكروت.
- prisma/seed-phase2-payments.ts (idempotent بمراجع ثابتة وفحص وجود): FeeRules الثلاثة YER (50/15/10 ثابتة) + 3 معاملات تاريخية لأحمد عبر postEntries (فاتورة كهرباء 5,000+50، شحن MTN 1,000+15، كرت يُو 9,000+10 برمز مولّد) بإشعارات + **ServiceState: BILLS/TOPUP/NETWORK_CARDS → ON «خدمة Beta مفعلة»** + ledgerCheck نهائي. شُغّل مرتين (الثانية تخطّت الكل ✓) — رصيد أحمد YER بعد البذرة 220,397.
- استبدال الشاشات المؤقتة الأربع بالكامل (نمط الشاشات الذهبية: loading/تعطيل بdisabledReason/InlineErrorBanner/ErrorState بالكود/PinStep/ReceiptCard/SuccessMark/postMoney/refreshMe/toast):
  • bills-screen: بحث + أقسام الفئات الخمس بأيقونات + «آخر مدفوعاتك» (types=BILL_PAY — النقر يفتح المزود مع تمليء الحساب).
  • bill-pay-screen: إدخال الحساب (تلميح الصيغة، تمليء من آخر المدفوعات) → استعلام → بطاقة المستحق + خيارا المستحق/مخصص (AmountPad) → مراجعة → PIN → إيصال.
  • topup-screen: شبكة المشغلين → «لهذا الهاتف»/«رقم آخر» (تحقق البادئة) + فئات chips → مراجعة → PIN → إيصال.
  • cards-screen: باقات بالمشغل → مراجعة (الرسوم الحية) → PIN → BigCodeCard للرمز بزر نسخ وQR وتحذير «لن يظهر الرمز مرة أخرى إلا في تفاصيل العملية» + إيصال.
- التحقق: bun run lint نظيف؛ curl (بجلسة x-sw-session): كتالوجات ✓، preview صحيح/خطأ/BIL-001 ✓، حتمية المستحق ✓، pay + replay نفس المرجع + TXN-003 ✓، PIN-001/TOP-001/TXN-001 (فاطمة)/TXN-002 (فاطمة بعد تحويل حقيقي 40,000)/GEO-001 (سامي) ✓، شراء كرت + replay = نفس الرمز ✓، ?ref= الثلاثة ✓، فلتر types ✓، ledger-check balanced=true (147 قيداً/52 مجموعة) ✓.
- متصفح agent-browser (سطح مكتب + جوال 390px): دخول أحمد → الخدمات (الثلاث متاحة بلا «قريباً») → سداد فاتورة كاملة حتى الإيصال وتفاصيلها برقم الحساب → شحن 500 لرقمه → شراء كرت سبأفون → رمز الكرت بزر نسخ → التفاصيل تعرض نفس الرمز المخزن (تحقق مطابقة مع DB) → جوال بلا تجاوز أفقي → فحص VLM للقطات: RTL سليم، هوية أسود/ذهبي، لا أزرق. اللقطات في worklog-attachments/9b/ (9 لقطات).
- ملاحظات تشغيل: الخادم توقف مرة أثناء الجلسة (عملية dev قُتلت خارجياً — dev.log خالٍ من الأخطاء) فأُعيد تشغيله bun run dev؛ وFast Refresh من تعديلات الوكلاء المتوازيين أعاد التوجيه للرئيسية مرتين (سلوك dev معروف بلا أثر وظيفي).

Stage Summary:
- المخرجات: خدمات المرحلة 2 الثلاث تعمل فعلياً E2E — 5 مسارات API بقيود مزدوجة Σ=0 (وجهة المبلغ SUSPENSE:YER بانتظار تسوية المزود — قرار 1) + بذرة idempotent (رسوم حقيقية من FeeRule قابلة للتحرير بM10 + سجل تاريخي + تفعيل ServiceState) + 4 شاشات كاملة + إثراء تفاصيل العملية بالأنواع الجديدة.
- قرارات موثقة: (1) SUSPENSE وجهة مبلغ الخدمات لا FEE؛ (2) بادئات مراجع جديدة BP/TU/NC؛ (3) استرجاع رمز الكرت/الـmetadata عبر ?ref= على مسارات 9-b (transactions/[ref] وapi-types مغلقان — الحل الوحيد المتوافق بملكية الملفات)؛ (4) الرسوم تُقرأ حية في الواجهة لا قيم ثابتة؛ (5) مزودات سداد كامل (اتصالات/إنترنت/حكومي) مقابل جزئي (كهرباء/مياه)؛ (6) بطاقة الخدمة في الرئيسية تفتح Sheet «متاحة» بدل التنقل — سلوك home-screen (ملك 8-b) للخدمات ذات stateKey، والمسار الذهبي عبر شاشة الخدمات يعمل — تُرك للمنسق.
- الحالة: **9-b مكتمل — الفواتير/الشحن/الكروت مفعلة ON ومختبرة curl + متصفح + دفاتر متوازنة. بانتظار 9-c و9-d لتكملة المرحلة.**

---
Task ID: 9-c
Agent: general-purpose (9-c)
Task: خدمات المرحلة 2 كاملة (خلفية + واجهة): دفع التاجر QR (MERCHANT_PAY + بوابة نقطة البيع) والحوالات الواردة من شبكات الصرافة (REMITTANCE_IN — A-03)

Work Log:
- قراءة worklog.md كاملاً (خصوصاً 9-0: قناة x-sw-session) + MVP_CONTRACT (الغلاف/الأخطاء/Idempotency) + الأنماط الذهبية (transfers/remittances/agent-remittance-pay + cashflow/money/ledger/pin/idempotency + transfer-screen/remittance-create-screen/agent-queue + عناصر ui المشتركة).
- **خادم — دفع التاجر**:
  - `src/lib/server/merchant-catalog.ts` (جديد): سجل التجار التجريبي (فئة/محافظة) + toMerchantPublicView (shopName من user.fullName أساساً والكاتالوج fallback).
  - `POST /api/merchant/lookup` {payload}: يقبل SWPAY:<phone> أو الرقم؛ أي حالة غير (MERCHANT + ACTIVE + غير scopeRestricted) أو رقم غير موجود → MRC-001 (بلا تسريب وجود الحسابات) → QrPayPreviewView.
  - `POST /api/merchant/pay` {merchantPhone, amountMinor, note?, pin} + Idempotency-Key: النموذج الثنائي الكامل (جلسة/تجميد/نطاق AC-07/Idempotency/PIN خارج المعاملة ثم $transaction واحدة): رصيد TXN-001 + حدود TXN-002 + FeeRule MERCHANT_PAY → **الرسوم على التاجر** (net = amount − fee، العميل يدفع المبلغ كاملاً) + قيود Σ=0: [MAIN العميل DEBIT a] + [FEE:YER CREDIT f] + [MAIN التاجر CREDIT net] + معاملتان (MERCHANT_PAY_OUT للعميل بfeeMinor=0، MERCHANT_SALE_IN للتاجر بnet وfeeMinor=f، relatedRef) + إشعاران + QrPayResultView (merchantName + balanceMinor). TRF-002 للدفع لنفسه، MRC-001 لتاجر غير صالح، TXN-003 لتعارض Idempotency.
  - `GET /api/merchant/pos` (دور MERCHANT فقط): إحصاءات اليوم بتوقيت عدن (عدد/حجم إجمالي/صافي/رسوم) + آخر 10 مبيعات + الرصيد المستحق للتسوية (مجموع net لكل MERCHANT_SALE_IN المكتملة) + بيانات رمز الدفع. RBAC-001 لغير التاجر.
- **خادم — الحوالات الواردة (A-03)** بحيلة تخزين موثقة بلا تعديل المخطط:
  - `src/lib/server/remit-in-store.ts` (جديد): الحيلة = سجل Remittance عادي لكن senderId=الأدمن المُصدر، receiverName/Phone=المستلم الحقيقي (المطالبة تتحقق user.phone==receiverPhone)، deliveryCode=رمز المطالبة، payingAgentId فارغ دائماً؛ والفرق (الشبكة/المرسل الحقيقي) في **معاملة نائبة PENDING** بنفس المرجع RI-… (userId=المستلم، type=REMIT_IN_CLAIM، counterpartyName=الشبكة، metadataJson {networkName, senderName}) — تحوّل عند المطالبة إلى COMPLETED وتُقيَّد تحتها القيود (نمط R2→G4 نفسه). + mapInboundStatus (PAID→CLAIMED، CANCELLED/EXPIRED→EXPIRED) + toInboundRemittanceView.
  - `src/lib/server/remit-in-networks.ts` (جديد): كتالوج 5 شبكات صرافة افتراضية + تحقق.
  - **حارس A-03 في `src/lib/server/cashflow.ts` (تعديل موثق ~20 سطراً في settleRemittance)**: عند غياب معاملة REMITTANCE ممولة بنفس المرجل → حوالة واردة → الإنهاء (انتهاء كسول أو إلغاء إداري M15) يقلب الحالة ويُشعر المستلم **بلا أي استرجاع نقدي** — وإلا لسحب المحرك من SUSPENSE أموالاً لم تدخل النظام (كسر الدفاتر). ضرورة هندسية لأن runLazyExpiry() العالمي يستدعى من مسارات عديدة (G4/M1/M14/…). اختُبرت بحوالة منتهية مُنشأة مباشرة: الحالة EXPIRED + إشعار المستلم + SUSPENSE لم يتغير + balanced=true.
  - `GET /api/remit-in`: قائمة حوالاتي الواردة (InboundRemittanceView[]) + إنهاء كسول. `POST` {claimCode, pin} + Idempotency: RIN-001 للرمز الخاطئ/المملوك لغيرك (رسالة موحدة بلا تسريب)/المنتهي/المطالب سابقاً → $transaction: قلب Remittance→PAID (بلا payingAgentId) + قلب المعاملة النائبة→COMPLETED (تخزن مفتاح المطالبة للreplay) + قيود Σ=0 **[FX:YER DEBIT a] + [MAIN المستلم CREDIT a]** — التمويل من محفظة FX (النقد الخارجي للاقتصاد المغلق D-01) + إشعار → RemitInClaimResultView.
  - `GET/POST /api/admin/remit-in` (ADMIN/COMPLIANCE): قائمة كل الواردة بفلاتر حالة؛ POST {beneficiaryPhone, senderName, networkName, amountMinor}: مستلم مسجل (TRF-001 لغيره) + شبكة من الكتالوج + رمز مطالبة 6 أرقام بفحص تفرد + expiresAt 7 أيام + إشعار وصول للمستلم (يشمل الرمز — قناة التسليم في Alpha) + AuditLog (REMIT_IN_ISSUE). feeMinor=0 في Beta (موثقاً).
- **واجهة الهاتف** (استبدال الملفات المؤقتة):
  - `my-qr-screen.tsx`: رمز استلامي كبير SWPAY:<هاتفي> + اسمي + مشاركة/نسخ + قسم «الدفع لتاجر»: زر «ادفع بمسح الرمز» (→ scan-qr) + إدخال يدوي لرمز تاجر → pay-merchant.
  - `pay-merchant-screen.tsx`: استقبال params.payload (lookup تلقائي) أو إدخال يدوي SWPAY → بطاقة معاينة التاجر (QR صغير + اسم/فئة/محافظة/هاتف) → مبلغ (YER فقط مع إظهار ذلك) + ملاحظة → مراجعة (الرسوم على التاجر بارزة) → PinStep → postMoney → إيصال ReceiptCard + مشاهدة العملية + refreshMe. عند MRC-001: زر «تحويل عادي إلى <الرقم>» → transfer (فصل المسارين).
  - `merchant-pos-screen.tsx`: رمز QR كبير للمتجر (SWPAY من /api/merchant/pos) + نسخ الرمز + 4 بطاقات إحصائية (مبيعات اليوم/الحجم/الصافي/المستحق للتسوية) + آخر 10 مبيعات بTransactionRow + EmptyState أنيقة + ErrorState/Skeleton.
  - `remit-in-screen.tsx`: شرح الخدمة + حقل رمز المطالبة OTPInput (6 خانات) + قائمة البطاقات (شبكة/مرسل/مبلغ/حالة مخصصة/انتهاء/الرمز) + زر «استلام» على المعلقة يفتح مراجعة+PIN → إيصال الاستلام + refreshMe + تحديث القائمة. EmptyState/ErrorState/Skeleton.
  - `scan-qr-screen.tsx` (تعديل): المسح والإدخال اليدوي يوجهان دائماً إلى pay-merchant بpayload (وهو يفصل تاجراً/مستخدماً عادياً بزر التحويل العادي) + أزرار محاكاة: العملاء → transfer كما كان، تجاران (متجر الجنوب/صيدلية الشفاء) → pay-merchant.
  - `home-screen.tsx` (استثناء مسموح — إضافة واحدة فقط): بطاقة «نقطة البيع — رمز دفع متجرك» بارزة أعلى الشاشة عندما role==="MERCHANT" → merchant-pos.
  - `demo-login.tsx` (استثناء مسموح — عنصر واحد فقط): بطاقة {تاجر 770000020 — متجر الجنوب — نقطة البيع QR}.
- **لوحة الإدارة**: `sections/remit-in-section.tsx` (جديد بنمط sections الأخرى): KPIs (معلقة/مستلمة/منتهية/قيمة المعلقة) + نموذج إصدار (هاتف المستلم/اسم المرسل/شبكة select/مبلغ) عبر useActionRunner مع toast بالرمز + فلاتر حالة + بطاقات القائمة. تعديل أدنى: `console-nav.ts` (مفتاح remit-in + عنصر Landmark + إدراج في ADMIN وCOMPLIANCE) و`console-shell.tsx` (import + case واحد — ملاحظة: مفتاح التبديل في console-shell لا console-app الذي لم يحتج أي تغيير).
- **Seed**: `prisma/seed-phase2-merchant.ts` (جديد، idempotent — لا يمسح شيئاً): تاجران 770000020 «متجر الجنوب للأغذية» (عدن، بقالة) و770000021 «صيدلية الشفاء» (لحج، أدوية) بMAIN الثلاث + FeeRule MERCHANT_PAY YER (1% بحد أدنى 20) + حوالتان واردتان PENDING لأحمد (552214 «الفلوس للصرافة» 85,000 تنتهي بعد يومين، 771903 «الخليج للصرافة» 120,000 بعد 6 أيام) بمعاملتين نائبتين + إشعارا وصول + معاملة بيع تاريخية (أحمد→متجر الجنوب 12,500 برسوم 125) عبر postEntries Σ=0 + تفعيل ServiceState: MERCHANT_PAY وREMITTANCE_IN → ON. **اكتشاف وتوثيق تعارض**: الهاتفان 770000020/21 كانا وكيلين إضافيين في seed.ts الأساسي (صرافة المكلا/وكيل الوفاق) — يحوّلهما الـseed كاملاً (fullName للمتجر + حذف AgentProfile وعومهما بلا قيود دفترية أصلاً بعد تحقق تحوطي) — وكلاء Seed يصبحون 10.
- **التحقق**:
  - `bun run lint`: نظيف (قبل وبعد كل شيء).
  - curl بقناة x-sw-session (22+ فحصاً): lookup تاجر ناجح + MRC-001 (فاطمة/رقم غير موجود) + MRC-001 → دفع ناجح 15,000 + replay=true بنفس المفتاح + TXN-003 بحمولة مختلفة + TXN-001 لرصيد غير كافٍ + دخول التاجر → POS يعيد إحصاءات صحيحة (4 مبيعات، حجم 36,000، صافي 35,640، رسوم 360، مستحق 35,640) + RBAC-001 لPOS من غير تاجر + مدير يصدر حوالة واردة (رمز 291745) + قائمة المدير + قائمة أحمد (3) + claim برمز خطأ RIN-001 ثم الصحيح 552214 (+85,000 للرصيد بالضبط) + إعادة المطالبة RIN-001 + replay المطالبة=true + RBAC-001 لغير الإدارة + فحص الدفاتر balanced=true (149 قيداً/53 مجموعة بعد كل العمليات) + رجوعية بوابات الوكيل/المعلّق/نظرة عامة سليمة بعد حارس cashflow + قائمة الوكلاء 10 بعد التحويل.
  - Agent Browser (جلسة معزولة): دخول أحمد → الخدمات → «دفع التاجر» → my-qr → إدخال 770000020 → معاينة التاجر (اسم/فئة/محافظة/رصيد YER) → مبلغ 3,500 → مراجعة (الرسوم على التاجر) → PIN → إيصال SW-…-V4AUD9WR + الرصيد نقص → الحوالات الواردة → بطاقة الخليج 771903 → مراجعة الاستلام → PIN → «تم استلام الحوالة» +120,000 → الرصيد زاد بالضبط → دخول تاجر 770000020 (بطاقة التاجر) → الرئيسية تعرض بطاقة نقطة البيع → POS: QR كبير SWPAY:770000020 + الإحصاءات + 4 مبيعات حية → دخول مدير → قسم «الحوالات الواردة» في اللوحة → إصدار حوالة جديدة (RI-…-KN3GAC3D برمز 660688 لمحمد 770000004) → جوال 390px: remit-in وscan-qr وpay-merchant بلا تمرير أفقي (scrollWidth=390) والمسار التجريبي للتاجر يعمل.
  - آخر 40 سطراً من dev.log: لا أخطاء (الوحيد EADDRINUSE قديم من محاولة تشغيل ثانٍ — 9-b) وصفر استجابات 500.

Stage Summary:
- المخرجات: 10 ملفات خادم/شاشات جديدة + 5 تعديلات أدنى موثقة (cashflow حارس A-03، scan-qr التوجيه، home بطاقة POS، demo-login بطاقة تاجر، console-nav/shell قسم واحد) + seed مرحلة 2 idempotent.
- القرارات المعمارية الموثقة: (1) الرسوم على التاجر تحسم من مبلغه (العميل يدفع المبلغ كاملاً)؛ (2) تمويل الحوالات الواردة من محفظة FX:YER (النقد الخارجي للاقتصاد المغلق D-01) بدل SUSPENSE غير الممول؛ (3) معاملة نائبة REMIT_IN_CLAIM PENDING بنفس مرجع الحوالة الواردة تحمل الشبكة/المرسل (تقلب COMPLETED عند المطالبة) — أتاحت فصلاً نظيفاً عن حوالات R2 العادية وحارس cashflow؛ (4) حد معروف: MERCHANT_PAY_OUT لا يُضاف لأنواع الحدود المحسوبة في money.ts (ملف مشترك مغلق) — يفحص المبلغ فقط؛ (5) قناة G4 (دفع وكيل برمز) ستفشل ذرياً لو أُدخل فيها رمز مطالبة وارد (لا التباس عملياً في Alpha).
- الحالة: **خدمتا 9-c (دفع التاجر QR + الحوالات الواردة) مكتملتان خلفيةً وواجهةً ومختبرتان E2E — الدفاتر متوازنة بعد كل العمليات، وServiceState للمفتاحين ON.**

---
Task ID: 9-e (orchestrator — التكامل النهائي والتسليم)
Agent: Main Agent (Z.ai Code)
Task: دمج نتائج الوكلاء المتوازيين 9-b/9-c/9-d + إصلاحات التكامل + التحقق E2E الشامل للتسليم التجاري النهائي

Work Log:
- مراجعة تقارير الوكلاء الثلاثة (كلها خضراء): 9-b (فواتير/شحن/كروت: 5 مسارات + 4 شاشات + seed متوازن)، 9-c (دفع تاجر QR + حوالات واردة + بوابة تاجر + قسم إداري + seed)، 9-d (PWA كامل: manifest + 5 أيقونات + layout + focus/safe-area + تدقيق جودة).
- إصلاحات التكامل الثلاثة:
  (1) home-screen: الخدمات ذات stateKey كانت تفتح Sheet تفسيرياً حتى وهي ON — الآن تتنقل مباشرة عند state=ON (الفواتير/شحن رصيد من الرئيسية).
  (2) screen-router: مفتاح التركيب صار يشمل user.id — إصلاح خلل بيانات عالقة عند تبديل المستخدم (كانت رئيسية التاجر تعرض معاملات العميل السابق). تحقق: معاملات التاجر بعد التبديل = MERCHANT_SALE_IN صحيحة.
  (3) app-styles: sw-fade-in يبدأ من opacity 0.3 بدل 0 (220ms) — زوال وميض الإطار الفارغ بين الشاشات.
- التحقق E2E الشامل عبر Agent Browser (desktop + mobile 390px):
  - سداد فاتورة مياه عدن كاملة: استعلام (مستحق حتمي 20,125) → سداد كامل → مراجعة → PIN → إيصال ✓
  - استلام حوالة واردة (رمز 291745): بطاقة → تأكيد PIN → +60,000 ر.ي مع إيصال «من حمد صالح باعوم عبر برقة للصرافة» ✓
  - دفع تاجر QR: إدخال 770000020 → معاينة → مبلغ 2,500 → PIN → إيصال «إلى متجر الجنوب للأغذية» ✓
  - بوابة التاجر: دخول ببطاقة «تاجر» → بطاقة نقطة البيع في الرئيسية → شاشة POS (مبيعات حية صافية بعد الرسوم) ✓
  - لوحة الإدارة: قسم الحوالات الواردة → إصدار RI-20260912-6MZ3SPPS (رمز 463527) → استلام محمد عبر API: +18,000 بالضبط ✓
  - الجوال 390px: صفر تجاوز أفقي (الرئيسية + الخدمات)، التنقل السفلي يعمل، لقطات محفوظة worklog-attachments/final/ ✓
  - التذييل لاصق، بلا أخطاء Console، dev.log نظيف ✓
- الفحوص الختامية: bun run lint نظيف؛ ledger-check = balanced=true (159 قيداً، 57 مجموعة، صفر مخالفات) بعد كل عمليات الجلسة؛ ServiceState: الخدمات الخمس ON وOFFLINE معطل عمداً (R-14)؛ manifest.json وأيقونات PWA تُخدم 200.

Stage Summary:
- **المرحلة 9 (التسليم التجاري) مكتملة**: التطبيق الآن يشمل المرحلة 1 (Alpha: النواة المالية) + المرحلة 2 (Closed Beta: فواتير، شحن، كروت، دفع تاجر QR، حوالات واردة) + تغليف PWA.
- الحسابات التجريبية: عميل موثّق 770000001 / عميلة 770000002 / خارج النطاق 770000003 / تاجر 770000020 / وكيل 770000010 / مدير 770100001 / امتثال 770100002 / دعم 770100003 — PIN الافتراضي 123456.
- إجمالي الملكية الوظيفية: 40+ مسار API، 35+ شاشة، 6 أدوار فعلية (عميل/تاجر/وكيل/مدير/امتثال/دعم)، 3 عملات، دفاتر مزدوجة متوازنة، توثيق هندسي كامل (7 وثائق ~8800 سطر) داخل التطبيق.
- جاهز للتسليم النهائي للمستخدم.

---
Task ID: 10
Agent: Main Agent (Z.ai Code)
Task: رفع المشروع بالكامل إلى مستودع GitHub الرسمي (Southern-wallet-QTBSW) وتسليم نقطة انطلاق موثوقة للانتشار

Work Log:
- فحص حالة Git المحلية: 56 ملفاً معدلاً/غير ملتزم به بعد اكتمال المرحلة 9، بلا remote مضبوط، الفرع main وحيد.
- التحقق من المستودع البعيد عبر ls-remote بالتوكن: المستودع موجود ويتاح وصوله وهو فارغ تماماً (لا مراجع).
- تنظيف المستودع قبل الرفع:
  • إضافة `/tool-results/` و `*.pid` و `.zscripts/dev.pid` إلى .gitignore (مخرجات أدوات مؤقتة ليست جزءاً من التسليم).
  • إلغاء تتبع 40 ملفاً سابقاً في tool-results/ + ملف PID عبر git rm --cached (ملفات القرص بقيت سليمة).
  • إنشاء `.env.example` نموذجي بـ DATABASE_URL نسبي محمول (`file:../db/custom.db`) لأن `.env*` مستثنى أمنياً من التتبع — أي مستنسخ للمستودع يعرف كيف يضبط البيئة.
- الالتزام (commit) برسالة عربية وافية تصف حالة التسليم التجاري الكاملة (Alpha + Closed Beta + PWA + توثيق).
- إضافة remote: origin → https://github.com/Mohammed503-qtb/Southern-wallet-QTBSW.git (التوثيق بالتوكن لعمليات الدفع اللاحقة).
- الدفع: `git push -u origin main` — نجح من أول مرة (فرع جديد main على البعيد).
- التحقق الختامي: HEAD البعيد = HEAD المحلي = 46e7211c7a62c4b8270fe2d929ab8841f4749338؛ شجرة العمل نظيفة (0 تغييرات)؛ 336 ملفاً متتبعاً على البعيد؛ إجمالي 6 التزامات (commits) في تاريخ المشروع.
- لم تُجر أي تعديلات على الشيفرة/الوظائف — عملية رفع صِرفة بلا مساس بالتطبيق العامل (الخادم ظل يعمل على 3000 طوال العملية).

Stage Summary:
- **المشروع برمّته الكامل منشور الآن على GitHub**: https://github.com/Mohammed503-qtb/Southern-wallet-QTBSW.git — الفرع main مضبوط لتتبع origin/main.
- المستودع يحوي: كامل شيفرة التطبيق (src/)، مخطط Prisma + بذور المراحل، 8 وثائق هندسية، لوحة الإدارة، PWA، قاعدة البيانات المبذورة، وسجل العمل الكامل worklog.md.
- الفرع الرئيسي محدّث ومتزامن؛ أي أوامر تنفيذ لاحقة (استكمال الخطة) ستبدأ من قاعدة نظيفة ومتطابقة مع البعيد.
- الحالة: **الرفع مكتمل ومتحقق منه — بانتظار أوامر المستخدم لاستكمال التنفيذ بحسب الخطة.**
---
Task ID: 11-b
Agent: general-purpose (11-b)
Task: تدقيق جهوزية الإنتاج + وثيقة الفجوات PRODUCTION_READINESS.md

Work Log:
- قراءة worklog.md كاملاً (410 أسطر، المهام 1→10) + docs/ROADMAP.md (§2 المراحل وGantt و§4-§8 تفاصيل المراحل ومعالم M-0..M-8 و§10 جدول التتبع وقائمة §136) + docs/SRS.md (بطاقة الوثيقة، §2.3 الأدوار العشرة، §5.1 FR-ADM-002 MFA، §6 NFR-SEC/NFR-REL) + docs/MVP_CONTRACT.md كاملاً (قرارات D-01، عقد الجلسة x-sw-session، حدود Alpha) + ARCHITECTURE.md (§8 الأمان: JWT 15د + Rotation + Argon2id + Rate Limiting، ADR-004/005، §12.1 عزل السجلات الثلاثة).
- تدقيق كود فعلي بالأنماط (قراءة فقط — لم يُعدَّل أي ملف كود):
  • الجلسة: قراءة src/lib/server/auth.ts كاملاً — رمز عشوائي 48-hex جيد، لكن **لا expiresAt في نموذج Session** (schema.prisma:79-91) والرمز عبر ترويسة x-sw-session يخزَّن في localStorage ويبقى صالحاً بلا انتهاء خادمي؛ لا تدوير/لا كشف إعادة استخدام (مقابل ADR-005).
  • المصادقة: قراءة api/auth/otp + verify + demo-login — **devCode يعاد في الاستجابة (otp/route.ts:47) وdemo-login يمنح جلسة لأي هاتف Seed بلا تحقق بما فيها ADMIN 770100001** (قناتا Alpha الحرجتان).
  • PIN: pin.ts — scrypt (s2$salt$hash) بtimingSafeEqual وقفل تصاعدي 3→5د/6→30د (انحراف موثق عن Argon2id).
  • Idempotency: idempotency.ts — بصمة sha256 لcanonicalJson + TXN-003 + @@unique (متين).
  • الحدود/الرسوم: money.ts — computeFee/assertDailyLimit من DB بتوقيت عدن + FX بدقة BigInt.
  • الحماية: rg لrequireUser في كل src/app/api (83 ملف مسار) + قراءة transfers/route.ts وadmin/users/[id]/freeze كأمثلة — RBAC خادمي فعلي في كل مسار.
  • فحوص الغياب: rate.limit/RateLimit = صفر؛ helmet/csrf = صفر (لا middleware.ts أصلاً)؛ Sentry/pino/correlation = صفر؛ backup = صفر.
  • ServiceState: rg serviceState لا يظهر إلا في api/services وadmin/services — **مسارات الخدمات الخمسة (bills/pay, topup, cards, merchant/pay, remit-in) لا تفحص الحالة خادمياً**.
  • البنية: next.config.ts فيه typescript.ignoreBuildErrors:true؛ db:push --accept-data-loss؛ db/custom.db متتبَّع في Git؛ prisma/schema.prisma (21 جدولاً SQLite مقابل 78 جدولاً PostgreSQL في DATABASE_ERD)؛ package.json بلا أي أداة اختبار/مراقبة.
  • تكاملات: payments-catalog.ts (مستحق محاكى حتمي)، remit-in إصدار إداري يدوي، notify.ts (إشعارات داخلية فقط — لا FCM/SMS/WhatsApp)، KycSubmission.docName/selfieName نصيان فقط.
- كتابة docs/PRODUCTION_READINESS.md (جديد فقط — 232 سطراً): بطاقة وثيقة بنمط docs/* + §1 جدول M-0..M-8 بحالات وأدلة + الموضع الصريح (نهاية المرحلة 2 وظيفياً) + §2 الجاهزية الصلبة (مالية/أدوار/حالات/PWA بمراجع) + §3 الفجوات في 5 مجموعات (A1-A13 أمنية، I1-I6 بنية تحتية، P1-P6 تكاملات، O1-O7 تنظيمية، C1-C4 منتج) بجدول سداسي (الوضع بالدليل/المطلوب/الخطورة/المسار/الجهد) + §4 قائمة M-7 التسع (0/9) + §5 خطة إغلاق بموجات 0-4 (~14-16 أسبوعاً) متوافقة مع ROADMAP + §6 عشرة Quick Wins (مع استبعاد الرؤوس الأمنية و/health لأن المنسق يتولاهما بالتوازي) + جدول اعتماد.
- تحقق سلامة الوثيقة: 15 جدولاً كلها متسقة الأعمدة (فحص python آلي) — أصلح أثناءها أنبوبين حرفيين كانا يكسران جدولين.

Stage Summary:
- أهم 5 فجوات Blockers:
  1) قناة الدخول مفتوحة عمداً: devCode في استجابة OTP + demo-login بلا أي تحقق يصل حتى حساب المدير — أي نشر عام بمظهرها الحالي = تجاوز كامل (A1).
  2) لا Rate Limiting إطلاقاً (NFR-SEC-006) + لا MFA للإدارة ولا قيود IP (FR-ADM-002/NFR-SEC-009).
  3) نموذج الجلسة بلا انتهاء صلاحية خادمي + رمز خالد في localStorage عبر x-sw-session بلا تدوير (NFR-SEC-004 مقابل ADR-005).
  4) بنية أحادية غير قابلة للتشغيل العام: SQLite بلا نسخ احتياطي/استعادة/مراقبة/تتبع أخطاء/بيئة Staging/CI (NFR-REL-002..005, NFR-MNT-004/005, بوابة M-0/M-1).
  5) كل التكاملات محاكاة: SMS/OTP تجريبي، مزودو الفواتير/الشحن مستحق محاكى، شبكات الصرافة إصدار يدوي — Master §139/§140 يمنع تفعيلها في Production.
- مفاجآت سلبية إضافية: ServiceState غير مفروض في مسارات الخدمات (مفتاح الإطفاء شكلي تجاه الAPI)؛ typescript.ignoreBuildErrors:true؛ ملف قاعدة البيانات متتبَّع في Git؛ لا أحداث أمنية لمحاولات الدخول الفاشلة.
- مفاجآت إيجابية: محرك الدفتر Σ=0 مفروض برمجياً بإشارة معكوسة لAGENT_FLOAT؛ Idempotency ببصمة حمولة متين؛ RBAC خادمي في كل مسار؛ قفل PIN التصاعدي مدروس (العداد يثبت خارج المعاملة)؛ هيكل OTP (محاولات/انتهاء/إبطال) جاهز لربط SMS حقيقي.

---
Task ID: 11-a
Agent: general-purpose (11-a)
Task: حزمة النشر الفعلي — Docker/Compose/Caddy/نسخ احتياطي/CI/DEPLOYMENT.md

Work Log:
- قراءة آخر ~300 سطر من worklog.md (المرحلتان 1 و2 مكتملتان: 40+ مسار API، لوحة إدارة، دفاتر مزدوجة، الرفع إلى GitHub في Task 10) + package.json (سكربت build يبني standalone وينسخ .next/static وpublic داخله فعلاً) + next.config.ts (output: standalone) + رأس prisma/schema.prisma (sqlite + env DATABASE_URL) + ls prisma/ (seed.ts وseed-phase2-payments.ts وseed-phase2-merchant.ts) + Caddyfile الجذري (بوابة Sandbox :81 — لم يُمس) + .gitignore + .env.example + docs/ROADMAP.md رأساً لمحاكاة بطاقة الوثيقة.
- التحقق من وجود /api/health فعلاً (src/app/api/health/route.ts: SELECT 1 → 200 مع db:true أو 503) واستخدامه في HEALTHCHECK وcompose والـCI والوثائق كما هو.
- فحص معماري تجريبي قبل كتابة Dockerfile: (1) bunx prisma db push يعمل تحت bun؛ (2) بدون إدخال node_modules/.bin/prisma يذهب bunx إلى الشبكة ويجلب أحدث prisma (7.x — لا يملك أمر push أصلاً) فالإدخال المحلي إلزامي؛ (3) بناء «إغلاق تبعيات» prisma (35 حزمة: prisma + @prisma/* + .prisma/client المولد + c12/effect ورفاقها) واختباره بتشغيل db push ناجح من الشجرة المغلقة وحدها (bun node_modules/prisma/build/index.js)؛ (4) محركات debian-openssl-3.0.x تربط libssl.so.3/libcrypto.so.3 → تثبيت openssl في runner؛ (5) البذور تستورد ../src/lib/server/* بمسارات نسبية → وجوب نسخ src/ وtsconfig.json إلى الصورة؛ (6) عارض الوثائق الداخلي (/api/docs) يقرأ docs/*.md من process.cwd()/docs → وجوب نسخ docs/ إلى /app/docs؛ (7) لا استخدام لـnext/image → لا حاجة لـsharp وقت التشغيل.
- إنشاء الملفات العشرة الجديدة (صفر تعديل على أي ملف موجود): Dockerfile (متعدد المراحل على oven/bun:1؛ builder: bun install --frozen-lockfile → prisma generate → bun run build → سكربت إغلاق prisma داخل RUN heredoc بBuildKit؛ runner: curl+sqlite3+openssl، مستخدم bun غير جذري، نسخ standalone وshim وprisma وsrc وtsconfig وdocs وpackage.json، mkdir /app/db بملكية bun، ENV تشغيلية بلا DATABASE_URL، EXPOSE 3000، HEALTHCHECK عبر curl /api/health)؛ deploy/docker-entrypoint.sh (set -euo pipefail: تحقق DATABASE_URL → اشتقاق مسار القاعدة (مطلق أو نسبي إلى /app/prisma) → bunx prisma db push --accept-data-loss عند غياب الملف مع احتياط تنفيذ CLI مباشر → معالجة HOSTNAME فخ Docker → exec bun .next/standalone/server.js)؛ docker-compose.yml (app + caddy:2-alpine، شبكة sw-net جسر داخلية، app بexpose فقط بلا ports، volume app-db، env_file .env.production للخدمتين (يمرر SW_DOMAIN إلى {$SW_DOMAIN})، depends_on service_healthy، init: true، restart unless-stopped، healthcheck)؛ deploy/Caddyfile (نطاق {$SW_DOMAIN} مع تعليمات ضبط عربية كومنتات، HTTPS تلقائي Let's Encrypt + بريد {$SW_ACME_EMAIL}، redirect HTTP دائم، encode zstd gzip، HSTS، reverse_proxy app:3000 بتمرير X-Forwarded-Proto وtransport مهلات dial 5s/response 60s، /api/health متاح للمراقبة الخارجية عمداً)؛ deploy/backup.sh (وضعا direct/compose، PRAGMA integrity_check قبل وبعد مع .timeout 5000، sqlite3 .backup متسقة والاحتياط cp مع نسخ WAL/SHM، دوران 7 يوميات + 4 أسبوعيات بترقية سبتية/غياب أسبوع، بصمة sha256 جانبية، سجل backup.log، قفل تشغيل)؛ deploy/restore.sh (sha256 + integrity → لقطة أمان pre-restore → stop → استبدال عبر compose cp → تنظيف WAL/SHM عبر compose run → start → فحص صحة حلقي 60ث → تراجع تلقائي عند الفشل، ووضع --drill لاختبار دوري بلا مساس بالإنتاج يعرض أعداد الصفوف — بوابة NFR-REL-002)؛ .dockerignore (node_modules/.next/.git/db/السجلات/مرفقات وسجلات الوكلاء/بيئات Sandbox وz/الاختبارات + .env* مع استثناء !.env.production.example + ملفات النشر ذاتها)؛ .github/workflows/ci.yml (on push/PR إلى main؛ job lint وjob build على ubuntu-latest عبر oven-sh/setup-bun@v2 وactions/checkout@v4 الرسميين فقط، bun install --frozen-lockfile، bun run lint، وبناء إنتاجي كامل مع DATABASE_URL=file:./ci.db وهمي + bunx prisma generate قبل build لأن الاستيراد يتكسر بدونه — بلا أي أسرار)؛ .env.production.example (عربي/إنجليزي: NODE_ENV/PORT/HOSTNAME=0.0.0.0 (إلزامي standalone داخل حاوية)/DATABASE_URL=file:/app/db/custom.db بلا علامات اقتباس (compose يمرر env_file حرفياً)/SW_DOMAIN (مثال)/SW_ACME_EMAIL + تحذير أمني بعدم الالتزام بقيم حقيقية)؛ docs/DEPLOYMENT.md (550 سطراً ببطاقة وثيقة بنمط docs الحالية: §0 نظرة عامة + مخطط ASCII (Browser → Caddy TLS → app → SQLite volume + backups) + جدول مكونات الحزمة وقرارات معمارية؛ §1 متطلبات (2vCPU/4GB/40GB SSD موصى بها، Docker 24+، Compose v2، DNS A، 80/443)؛ §2 نشر أول خطوة بخطوة (clone → cp .env → up -d --build → تحقق → HTTPS تلقائي)؛ §3 المخطط التلقائي عبر entrypoint + البذور الثلاثة الفعلية بالأوامر (docker compose exec app bun prisma/seed*.ts) + جدول الحسابات التجريبية + تحذير إلزامي «البذور للتجربة فقط والنشر العام بقاعدة نظيفة»؛ §4 كرون يومي + خطوات استرجاع فعلي + --drill + RPO≤24h/RTO≤30min + جدول اختبارات الاسترجاع (NFR-REL-002)؛ §5 تحديث قياسي + rollback مبسط من Master §137 (تعطيل ميزة ← حالة آمنة ← rollback) + ملاحظة صفر توقف مع Caddy؛ §6 مراقبة (/api/health، Uptime Kuma + Sentry، p95≤2s وتوفر 99.5%)؛ §7 أمان (ufw مثال، SSH بمفاتيح، تحديثات، أسرار بصلاحية 600، قيود لوحة الإدارة لاحقاً)؛ §8 استكشاف أخطاء (سجلات + جدول مشاكل/حلول + إصلاح ملكية volume)؛ §9 قائمة تحقق M-7؛ §10 حدود النطاق (SMS/KYC/فواتير حقيقية/PenTest/تجار/PostgreSQL) بإحالة إلى docs/PRODUCTION_READINESS.md المُعد بالتوازي).
- التحقق: bash -n للسكربتات الثلاثة ✓؛ تحليل YAML (docker-compose.yml + ci.yml) عبر PyYaml ✓؛ مراجعة يدوية كاملة للـDockerfile وCaddyfile (لا Docker متاح في Sandbox — موثق كخطر متبقٍ)؛ اختبار تشغيلي فعلي آمن لأوامر الوثائق الحساسة: bunx prisma db push --accept-data-loss إلى قاعدة معزولة /tmp ثم bun prisma/seed.ts كاملاً إليها (نجح الزرع كاملاً بالجداول والأرصدة) مع تثليج مسبق لأسبقية متغير البيئة الصريح على .env في bun — قاعدة الإنتاج الحقيقية لم تُمس.
- اكتشافات وُثّقت: (1) نمط .env* في .gitignore يتجاهل .env.production.example → يتطلب git add -f أو سطر استثناء من المنسق (موثق في الملف نفسه وDEPLOYMENT.md §7.4)؛ (2) علامات الاقتباس في env_file تدخل القيمة حرفياً → مثال الإنتاج بلا اقتباس؛ (3) أحدث prisma على السجل (7.x) لا يملك db push — أهمية الإدخال المحلي .bin (وإلا يجلب bunx النسخة الخطأ)؛ (4) فخ HOSTNAME داخل Docker عولج ثلاثياً (صورة + env_file + entrypoint).

Stage Summary:
- الملفات المنشأة (10): Dockerfile، deploy/docker-entrypoint.sh، docker-compose.yml، deploy/Caddyfile، deploy/backup.sh، deploy/restore.sh، .dockerignore، .github/workflows/ci.yml، .env.production.example، docs/DEPLOYMENT.md — كلها جديدة، وصفر تعديل على أي ملف موجود (Caddyfile الجذري وnext.config.ts وpackage.json وsrc/ وprisma/ وdb/ وpublic/ لم تُمس، ولم يُلمس حتى .gitignore).
- القرارات المعمارية: (1) «إغلاق تبعيات prisma» (35 حزمة ~243MB) داخل الصورة بدل تثبيت كامل ليعمل bunx prisma db push وقت التشغيل بلا شبكة — مُتحقق منه تجريبياً؛ (2) نسخ src/ وdocs/ وtsconfig.json وprisma/ إلى الصورة لتشغيل أوامر البذور الموثقة وإبقاء عارض الوثائق الداخلي عاملاً؛ (3) DATABASE_URL وقت التشغيل فقط من .env.production (لا قاعدة بيانات داخل الصورة — المخطط عبر entrypoint عند أول تشغيل)؛ (4) /api/health متاح خارجياً عمداً للمراقبة عبر Caddy؛ (5) restore بتراجع تلقائي + وضع --drill كإجراء NFR-REL-002 قابل للأرشفة؛ (6) env_file واحد (.env.production) يغذي التطبيق وCaddy معاً فلا ازدواج ضبط.
- المخاطر المتبقية (موثقة في DEPLOYMENT.md §8 والتقرير): بناء الصورة الفعلي لم ينفذ هنا (لا Docker في Sandbox) — بوابة CI build عند أول push هي أداة الكشف؛ ترقية Prisma إلى 7 مستقبلاً ستكسر أمر db push (تعليمات الفحص في §8.2)؛ .env.production.example يحتاج git add -f من المنسق بسبب نمط .gitignore الحالي.
- الحالة: **11-a مكتمل — حزمة النشر الإنتاجي جاهزة للالتزام والرفع من المنسق.**

---
Task ID: 11
Agent: Main Agent (Z.ai Code — orchestrator)
Task: الانتقال بالإنتاج: استكمال خطة Beta الفنية (Service Worker + نطاق إنتاجي) + تحصين أمني + إصلاح الفجوات الحرجة المكتشفة + الاختبار الشامل + تحديد الموضع في خارطة الطريق

Work Log:
- إطلاق وكيلين متوازيين: 11-a (حزمة النشر: Docker/compose/Caddy/backup/CI/DEPLOYMENT.md — 10 ملفات جديدة) و11-b (تدقيق جهوزية: PRODUCTION_READINESS.md + اكتشاف 5 فجوات Blockers قابلة للإصلاح بالكود).
- **Service Worker كامل (Beta)**: public/sw.js (نسخة sw-beta-1.0.0) — مبدأ مالي حاكم: لا اعتراض لغير GET ولا لمسارات /api أبداً؛ تنقلات شبكة-أولاً بلا تخزين HTML إنتاجي (تفادي قشرة قديمة)؛ cache-first لأصول _next/static/icons/fonts؛ SWR بسقف 60 للبقية؛ navigationPreload؛ SKIP_WAITING برسالة فقط. public/offline.html بهوية أسود/ذهبي بلا JS (متوافقة CSP) + محاولة تلقائية كل 30 ثانية. src/components/app/shell/sw-register.tsx: تسجيل ذكي (يتجاهل localhost حفاظاً على Fast Refresh إلا مع ?sw=force؛ خارج localhost وفي الإنتاج يسجل دائماً؛ تعطيل عبر NEXT_PUBLIC_SW_DISABLED) + فحص تحديث كل 6 ساعات + إشعار تحديث بموافقة المستخدم (toast + ToastAction) + إعادة تحميل واحدة بعد controllerchange. مركّب في layout.tsx.
- **التحصين الأمني**: next.config.ts — poweredByHeader:false + compress + رؤوس أساسية دائماً (nosniff/referrer/permissions-policy) وإنتاجية فقط (HSTS + X-Frame-Options DENY + CSP متوازنة مع Next). src/app/api/health/route.ts — نقطة فحص جاهزية عامة (SELECT 1 + 503 صريح عند فشل القاعدة).
- **إصلاح الفجوات الحرجة الخمس (من تدقيق 11-b)**:
  1) إنفاذ ServiceState خادمياً (Master §139): src/lib/server/service-guard.ts (assertServiceOn — مفتاح غير موجود = معطّل افتراضياً) مطبق على 6 مسارات مالية (bills/preview+pay, topup, cards, merchant/pay, remit-in) برمز SRV-001/503.
  2) بوابة مصادقة التطوير: src/lib/server/runtime.ts (isDevAuth) — devCode لا يُعاد في استجابة OTP بالإنتاج، وdemo-login محجوب كلياً (AUTH-006) + إخفاء الواجهة في بناء الإنتاج (دفاع مزدوج).
  3) انتهاء الجلسات: Session.expiresAt (7 أيام موحدة لقناتي الكوكي والترويسة — كانت الترويسة خالدة) + إنفاذ خادمي في getCurrentSession مع إبطال كسلي + تنظيف عند كل دخول + فهرسة [active,expiresAt]. db:push ناجح.
  4) حد المعدل: src/lib/server/rate-limit.ts (نافذة ثابتة بالذاكرة — موثق لعقد واحدة) على otp (12/س/IP) وverify (30/س/IP) وdemo-login (20/س/IP) برمز SYS-002/429.
  5) الرموز الجديدة: SYS-002/SYS-003/AUTH-006 في ERROR_MESSAGES + أنواع devCode string|null.
- إضافة الوثائق الجديدة لعارض التطبيق: docs-manifest (خطوتان 8 و9 بأيقونات Rocket/ShieldCheck) + أيقونات docs-screen — التطبيق يعرض الآن 9 وثائق (النشر والجهوزية داخل التطبيق).
- بطاقة حالة مؤرخة أُضيفت لرأس ROADMAP.md (موضعنا الحالي M-0..M-8 بلا تجميل) + استثناء !.env.production.example في .gitignore.
- **الاختبار الشامل**: bun run lint نظيف. مصفوفة curl (16 فحصاً): OTP+devCode بالتطوير، verify صحيح/خاطئ (AUTH-002)، انتهاء جلسة يدوي → 401 AUTH-001 + إبطال فعلي، حد معدل demo-login (20 ثم 429) وOTP، إنفاذ ServiceState (إطفاء BILLS → SRV-001 503 على preview+pay، تشغيل → عمل طبيعي)، سداد فاتورة كاملة BP- + replay=true + PIN-001، تحويل SW-، شحن TU-، كرت NC-، دفع تاجر (رسوم على التاجر)، POS التاجر، admin/overview، RBAC-001 403 للعميل، إصدار+استلام حوالة واردة RI-، ledger-check balanced=true (178 قيداً/64 مجموعة/صفر مخالفات). /api/health وsw.js (MIME صحيح) وoffline.html جميعها 200.
- **اختبار المتصفح E2E (SW كامل)**: ?sw=force → تسجيل وتحكم فوري؛ الكاش: 9 أصول مسبقة + كل أجزاء التطبيق (45+ إدخالاً)؛ **محاكاة الأوفلاين الحقيقية** (set offline on + reload) → صفحة offline.html المخصصة بالهوية؛ عودة الاتصال + زر المحاولة → التطبيق يعود بجلسة حية؛ أوفلاين أثناء الاستخدام → ErrorState النظامية (إعادة المحاولة/الدعم) بلا انهيار؛ **دورة التحديث كاملة**: رفع الإصدار 3 مرات → update() → waiting → إشعار «تحديث جديد متاح» → نقر «تحديث الآن» → تفعيل (waiting:none/activated) + حذف الكاشات القديمة تلقائياً + إعادة تحميل؛ جوال 390×844 بلا تمرير أفقي (scrollWidth=390 بالضبط)؛ صفر أخطاء كونسول/صفحة؛ dev.log نظيف (العمود expiresAt ظاهر في الاستعلامات)؛ تنظيف بيئة الاختبار (إلغاء تسجيل SW + حذف الكاشات) للسماح بتطوير لاحق نظيف.
- إعادة تشغيل خادم التطوير مرتين أثناء الجلسة (امتلاء حدود الذاكرة من الاختبارات + توقف عابر) — تعافٍ نظيف والحالة 200 دائمة بعدها.

Stage Summary:
- **خطة Beta الفنية مكتملة**: Service Worker إنتاجي كامل بدورة حياة مُختبرة E2E (تسجيل/أوفلاين/تعافٍ/تحديث بموافقة المستخدم/تنظيف كاش)، وحزمة نشر فعلية جاهزة (Docker+Caddy+CI+نسخ احتياطي مع drill) — البوابة المتبقية للنطاق الإنتاجي شراء نطاق وتشغيل docker compose (وثقت خطوة بخطوة في DEPLOYMENT.md).
- **خمس فجوات Blockers أُغلقت بالكود** (كانت ستتيح تجاوز مصادقة كامل في أي نشر عام): قنوات التطوير محجوبة بالإنتاج، الجلسات تنتهي، حد المعدل مفعل، مفتاح الإطفاء الإداري مُنفّذ خادمياً، رؤوس أمنية + CSP + health.
- المخرجات الجديدة: 13 ملفاً (2 من الوكلاء + 11 لي) + 12 تعديلاً موثقاً — lint نظيف، الدفاتر متوازنة بعد كل الاختبارات (178/64/0)، كل المسارات خضراء.
- الموضع في الخارطة (بطاقة حالة في ROADMAP.md): **نهاية المرحلة 2 وظيفياً — بوابة M-6 جزئية (الأساسات الأمنية منجزة) — M-7 البوابة التالية (0/9 من قائمة §136) والفجوات المتبقية خارجية بالكامل (نطاق/SMS/مراقبة/PenTest/عقود مزودين)** — التفصيل في PRODUCTION_READINESS.md بخطة إغلاق 5 موجات.
- الحالة: **المهمة 11 مكتملة — التطبيق جاهز للتسليم مع حزمة النشر؛ بانتظار أمر المستخدم للرفع إلى GitHub.**
